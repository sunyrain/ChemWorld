# Retained EQ figure history

2026-09-23: the user approved the simplified five-world comparison. The Chinese NCS source used `eq_predictions_reference.png`; `eq_predictions_reference.svg` retained editable vector elements. These files remain exact copies of the approved [preview](../../../output/figures/eq-simple-preview/README.md).

2026-09-25 remote contribution: a three-panel retained-data alternative combines regime MAE, interval coverage and the five-world Q08 comparison. Its compatibility PNG is `../final-ppt/figure05-eq-evidence.png`. Reproducible PDF/SVG/TIFF/PNG exports and source data are stored as `figure05-regime-bars-reference-style.*` and are generated by `../../tools/render_figure05_regime_bars.py`. All these remote assets are retained.

Current integrated binding: the English and Chinese manuscript sources use `figure05-eq-coverage-reversal.pdf`, generated by `../../tools/render_eq_closeout.py`. This version shows source evidence coverage, regime reversal and the five-world most-dilute forecasts. The earlier simplified preview and remote three-panel alternative remain separately reproducible. The [current figure set](../final-ppt/README.md) records the manuscript bindings.

Main Table 1 now carries the three-arm, two-regime MAE/coverage summary as a real table. [Supplementary Table F1](../../venues/ncs/eq_prediction_details.md) preserves all fifteen original prediction intervals and source ranges. Main-text captions distinguish pooled research observations, point predictions, evaluator reference means and three-response aggregates. No result or uncertainty interval was discarded.

To reproduce the active manuscript asset, run `uv run --no-sync python paper/tools/render_eq_closeout.py`. To reproduce the remote alternative, run `uv run --no-sync python paper/tools/render_figure05_regime_bars.py`. To reproduce the historical simplified asset, run `uv run --no-sync python paper/tools/render_eq_simple_preview.py`. These are retained-data figure production without model or simulator calls.
