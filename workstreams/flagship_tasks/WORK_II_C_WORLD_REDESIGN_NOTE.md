# C world/task setting repair — development block, 2026-09-20

Question: does the existing latent material family support legal, informative and quality-balanced research across worlds when reference conditions are specified completely? This user-authorized block repairs the task/reference setting before considering any change to physical laws. Keep full-process v5, all nominal material profiles, world residuals, quality thresholds, resolutions, E-arm intervention and 12/24 source budgets unchanged. No provider calls or agent-performance-based selection.

Fix the reference boundary condition explicitly: after the existing quench, equilibrate the quenched mixture to 300 K for 300 s with the public heat operation, then add seed. This operation consumes time/energy and is included in every query. Quenched thermal treatment does not advance reaction chemistry. No implicit clamping, rejected-action repair, hidden intervention or imposed source-agent recipe. Also correct the remaining public operational-state text that incorrectly calls the C recovery denominator original reagent charge.

Calibration coverage fixed before execution: 36 recipes (three paired variants for each of solvent, seed, cooling history, thermal history, continuing growth, upstream loading) in three development worlds, seeds 17, 23 and 31: 108 single-batch executions plus exact replay. The first 24 recipes are the retained candidate definitions with the explicit 300 K preconditioning; twelve additional recipes explore longer growth/maturation within the same public operation limits. Save every action list before the first run. Retain every failure and outcome. Individual recipes must fit the existing 60-operation reference envelope.

Choose one common pair per factor using only these three development worlds; require twelve unique complete recipes. In every development world require >=3 quality-positive and >=3 quality-negative cases, >=1 quality-feasible recovery >=0.10, >=4/6 resolved factor pairs, legal execution and exact replay. Rank eligible combinations by worst-world resolved-pair count, minimum feasibility witnesses, worst-world class balance, then summed effect strength; deterministic candidate order breaks ties. Preserve all candidates and the selection rule. Do not relax thresholds or tune simulator coefficients to obtain a pass.

If calibration passes, seal the chosen twelve recipes before a provider-free development acceptance block on the five existing planned worlds (seeds 0-4): 60 query batches plus 15 identical O/A/M probes, with exact replay, public anonymity, metric wording, budgets, physics/observation equality and distinct world instances. These five worlds have already been inspected during earlier qualification, so this is development acceptance, not fresh confirmatory evidence. Their outcomes cannot change the selected recipes within this block. A failed acceptance stops without launching agents. A future formal block needs a fresh source binding and full qualification from its first unit under the repository contract.

Measurements: noise-free recovery, purity, size index, fines; quality and positive-recovery feasibility separately; pair effects relative to existing resolutions; execution failures, actual operations/final assays, replay, wall time and resource consumption. All data under a new ignored development root; readable English report and machine summary under Work II reports. Emit completed/total, throughput and ETA after each recipe plus the existing within-recipe heartbeat. Initial 108 batches; conditional additional 75, total at most 183 before replay. Do not modify or overwrite the old calibration, v1/v2 qualifications or pilot sessions.

## Completed v1 and a separate domain diagnostic

The first block completed 108 calibration and 75 acceptance batches. Acceptance passed four of five worlds. W04 passed execution, replay, information isolation and factor coverage, but had zero quality-positive references: every fines fraction exceeded 0.50. Preserve this negative result. It does not establish that the whole world is infeasible.

Before the next execution, fix a separate provider-free diagnostic: four solvents x three reagent charges (0.004, 0.006, 0.008 mol) x three solvent volumes (0.020, 0.028, 0.040 L), 36 recipes in each of seven worlds (the five existing seeds 0-4 and two additional development seeds 41, 47), 252 executions and exact replays. Hold catalyst 1, seed 0.045 g, charged 300 K preconditioning and sixteen 14,400 s cooling segments to 278.15 K fixed. No change to physical laws, material residuals, quality or feasibility thresholds. Save the entire grid before execution. Maximum 60 operations per recipe. No selecting or excluding worlds based on results.

Question: can public loading/volume/material controls reach positive recovery with acceptable fines, and is initial supersaturation associated with the failed recipes? Record all four metrics, quality and recovery>=0.10 feasibility, exact replay, operations, time and resources. A host-only read-only diagnostic may record the initial supersaturation, maximum step supersaturation and particle count from crystallizer state; these quantities are never delivered to the agent. Report each world's complete denominator, count of feasible recipes and highest feasible recovery; zero feasible recipes is an explicit negative outcome, not a proof of global impossibility. Preserve all failures. This grid is an accessibility diagnostic, not a replacement twelve-query qualification and not a source-agent policy. No formal launch follows it automatically. Any subsequent query redesign requires a separately recorded fixed block before execution.

Completed domain diagnostic: 252/252 executions and exact replays, 6,804 operations and 6,804 replay steps. Feasible recipes per world: 11, 7, 9, 8, 10, 10, 10 (of 36 each). One common recipe, G01, is feasible in all seven, with recovery 0.2062-0.2958 and fines 0.2430-0.4765. The common witness uses S0, 0.004 mol reagent, 0.020 L solvent, catalyst 1, 0.045 g seed and the declared long cooling path. W04's best feasible recovery is 0.2688. Thus the original failed references did not establish physical infeasibility. Keep the physical family, world distribution and quality target; redesign query coverage before the next qualification. The witness remains host-only and is not an agent recipe or fresh confirmation evidence. No new model sources were launched.

Timing: 1,975.452 s summed execution/replay time (median 1.953 s, maximum 61.641 s per recipe). S2 accounts for 1,269.813 s of this total. Persistent particle cohorts and repeated growth solves make some conditions much slower than the previously benchmarked recipe; interface speedup does not imply uniform physical-solver latency. Do not change solver precision during an experimental block.

Validation: 100 of 101 focused checks passed on the first acceptance run. The new host-diagnostic equality test initially compared random campaign identifiers; after normalizing only those identifiers and independently verifying both resource ledgers, the remaining test passed. Ruff passed. No experimental outcome was rerun or replaced for this test-fixture correction. Shared-runtime changes were committed separately and pushed; this development world-design block remains local.
