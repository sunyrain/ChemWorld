# Work II A-S Study B3 gpt-5.6-sol medium replication preparation

Status: participant execution authorized by the user on 2026-08-28; frozen three-session canary is
required before the 30-session formal block.

## Question and coverage

Prepare an exact cross-provider replication of replicated B3 using `gpt-5.6-sol` with reasoning effort
`medium`. The evidence roster, five development worlds, five public worlds, three prior arms, two
independent sessions per arm/world, two-turn prompt, output schema, evaluator truth, structural
denominator and availability-aware action measurements match DeepSeek B3 v0.2. The intended future
participant denominator is 30 sessions and zero physical experiments.

## Preparation measurements and pass rules

- Resolve the exact model slug and medium reasoning setting without aliases.
- Validate the cached ChatGPT subscription login, Codex CLI availability, Responses transport,
  disabled tool envelope, structured-output schema, and same-thread resume command without making a
  participant call.
- Materialize provider-free qualification, public truth, and the 30-cell input manifest. Their roster,
  packet and truth hashes must match the DeepSeek v0.2 preparation outputs.
- Emit all static failures. Do not run a canary or formal session until the prepared block is reviewed
  and execution is separately authorized.

## Participant execution and stop rules

- The canary is replicate 1 of the first frozen public world across all three prior arms. It passes
  only if all three sessions complete both turns with identical public packets, valid pre/post typed
  laws, the exact eight-query scoring denominator, and a novel selected scoring-roster action.
- Participant/schema/scientific failures are retained without replacement and reject the block before
  formal execution. Only classified infrastructure failures may use the frozen limit of two retries.
- If and only if the canary passes, execute all 30 frozen cells (five worlds by three arms by two
  independent sessions). Any runtime or schema change after formal unit 1 requires restarting the
  complete formal block from unit 1; outcomes never select retries, worlds, arms, or replicates.
- Record provider attempts, completed turns, same-thread continuity, usage, tool events, structural
  recovery, unseen prediction error, selected true rank, Top-1, regret, and availability-aware gain.
  Participant physical experiments remain zero.

## Expected outputs

A versioned protocol, provider-free prepared output root, static harness report, canary summary, and,
only after a qualified canary, 30 immutable formal cell records plus the machine summary. Cross-model
comparison remains descriptive and must keep provider-specific denominators separate.
