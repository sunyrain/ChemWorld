# Shared runtime optimization (development, 2026-09-20)

Question: can redundant action-mask evaluation and copying of equipment histories be removed without changing any scientific or agent-visible behavior? This block makes no provider calls and does not qualify a new scientific design.

Coverage, fixed before execution: all 15 registered task IDs at seed 0, both reset and after the same solvent/reagent actions; one history-heavy crystallization state at seed 17 using the existing twelve-segment gentle-cooling recipe. Three view-generation timing repetitions per state, with a separate instrumented precondition-call count. Save the unmodified implementation's public views, action validations and state checks before editing. Compare the optimized implementation against these snapshots exactly. Preserve rejected actions as outcomes. Run the retained W01 qualification's 201-operation recipe through the normal runner after optimization and compare every public view, observation, action, reward, transaction result and campaign resource ledger with the retained trajectory; check exact replay separately. Historical elapsed time is a reference, not a controlled repeated benchmark.

Acceptance: no snapshot differences, no state mutation from view generation, no weakening of invalid-action/resource checks, independent returned data, and exact replay. Measure wall time and precondition evaluations; report all states, failures and denominators. No physics, solver timestep, tolerances, noise, experimental budgets or qualification thresholds change. Performance timing does not itself qualify worlds. Focused regression tests cover state changes, history projection isolation and complete public views.

Outputs: ignored development snapshots/timings under `runs/development/work-ii-runtime-optimization-20260920/`; one English summary/report under the Work II reports directory. The stopped C v2 qualification and all negative outcomes remain immutable. Formal model execution stays on hold until a separately frozen, scientifically valid full qualification passes. Clarify the future C prompt's recovery denominator to match the existing simulator; retain the original pilot's mistaken wording as historical evidence.

Development refinement before the second candidate measurement: the first optimized normal run took 199.079 s versus the historical 1,357.672 s. The remaining redundant work includes rebuilding/verifying resource snapshots for every field within one synchronous public-view request. Reuse one verified snapshot within that request only, then repeat the same comparison coverage and 201-operation recipe under a separate candidate output. Normalize random episode IDs and their derived resource receipt identifiers for cross-run comparison; independently reconstruct every resource receipt and ledger hash on both trajectories rather than treating differing UUID-derived hashes as scientific discrepancies. Preserve the first candidate and its initially unnormalized comparisons. No experimental coverage or acceptance threshold changes.

Final outcome: all 31 public-view/state comparisons passed; the final normal path took 19.141 s including replay, with 201/201 exact steps, zero physical error, identical 12-batch outcomes, and all original/candidate resource receipts and hashes independently verified. Focused acceptance passed 132 tests across public interfaces, resource accounting, full-process physics, exact replay and C runners. Ruff passed for the changed Python files. Development checks also exposed an extra recovery-hint recomputation (removed), a stale test expecting a real material name after C anonymization (corrected), and a test fixture using the wrong metric-contract key (corrected). No scientific failure or experimental threshold was revised. Full-process timings are measured on one retained C recipe block, so other systems' end-to-end speedups and provider latency require their own measurements.
