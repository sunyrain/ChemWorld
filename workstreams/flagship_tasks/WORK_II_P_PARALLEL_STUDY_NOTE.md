# P twelve-batch parallel study and closure

Authorization: on 20 September 2026 the user requested maximum parallel completion of P,
then study closure. This supersedes the earlier C-first scheduling restriction. Development
evidence; GPT-5.6 Sol / medium, English prompts and reports, no 24-batch branch.

Question: how do E-locus Opaque/Aligned/MisIndexed information conditions affect autonomous
purification, its free mechanistic account, blind prediction, and recommended recovery at
purity >=0.80? The native composite score is diagnostic, not the commission.

Fixed coverage: the same five worlds and E dossiers used by the passed P v3 gate, one
independent source per world/arm, 12 batches each: **15 sources, 180 source batches,
45 K1/Q/K2 stages, 15 recommendation retests**. First run one separate W01/Opaque integration
pilot with 12 batches, three posttests and one retest. It is an additional development cost,
not a selected replacement for the matrix's W01/Opaque. Technical path acceptance alone
releases the full matrix; low recovery or inaccurate scientific answers do not block launch.

Use the already-qualified P public/measurement adapter in source, replay and retest. Keep
world physics, six prediction pairs, resources and anonymous dossiers unchanged. Each source
has 12 extra measurements, 12 final assays and 720 operation attempts, with the existing
shared inventory card. The agent chooses operations, measurements and experiment grouping;
operations within its physical campaign are serialized. No candidate-mechanism menu,
mandatory belief checkpoints or equation form is added.

Seal the completed-batch recommendation, then request the existing generic English EC K1,
P Q and generic EC K2 in that order on the original thread. Q has the 12 fixed P v3 recipes,
with purity and original-charge recovery point estimates and 80% prediction intervals.
Disclose 128 calculator attempts per posttest, including invalid attempts and transport
continuations. No laboratory calls or hidden file access during posttests. Retain all answers.
Use the existing v3 Opaque reference observations at observation seed 101, with their exact
trajectories and namespace, as the fixed blind targets; no new reference selection or physics.
Source observation seed is 0; each recommendation retest uses 303. Reused 60 reference
batches are historical shared costs, not 60 newly executed experiments.

Score both metrics by MAE, interval coverage, width and interval score; retain all six paired
directions with 0.02 resolution, including small-effect controls. Report purity eligibility
and recovery separately for the chosen source batch and retest. Retest the committed actions
of the selected completed batch, omitting and explicitly retaining rejected attempts. A
rejected action is not executed in a fresh vessel. An absent/invalid recommendation remains
an outcome. Do not invent a replacement recipe. K1/K2 remain qualitative artifacts.

Execution: one coordinator with a maximum of 15 isolated provider-worker processes, one
cell per process, after the separate pilot. No nested provider concurrency. Source and all
follow-ups remain sequential within a cell. Progress at least every 30 seconds reports
stages, batches, completed/total cells, throughput and conditional ETA.

Recovery: preserve every attempt. Only diagnosed transport failure before posttests permits
a fresh source session, with exact replay of any retained prefix, in a separate attempt
directory; never append a guessed source state. Back off 60/120/240/480/900 seconds, cap
at six attempts per cell. An interrupted posttest resumes its original thread, retains
earlier sealed answers and the cumulative calculator audit, with the same bounded retry
schedule. Authentication/quota, schema/scientific failure, source budget exhaustion and
unfavorable outcomes are not transport retries. Limit retries and retain terminal failures
rather than indefinitely occupying slots. A coordinator restart resumes only never-started
or explicitly eligible interrupted work and does not repeat completed cells.

Technical acceptance: actual source/tool path, public anonymity, correct dossier/lifecycle,
exact replay, readable resource ledger, same-thread K1/Q/K2 payloads and faithful retest
execution. A platform/contract defect is repaired in a separately labelled block from its
first unit; original results remain. A postprocessing defect is corrected from intact
artifacts without model calls. No global clean-worktree or historical certificate gate.

Before provider execution, run one fixed provider-free acceptance campaign through the new
source/retest driver: the twelve qualified v3 recipes in W01/Opaque with source seed 0,
followed by the first recipe through the one-vessel retest path at seed 303. These 13 batches
are separately recorded integration costs, not new query references or agent evidence.

## First real-path incident and correction

The 13-batch provider-free driver acceptance passed with exact replay (220 operations).
The first model pilot then stopped before executing any laboratory operation: its driver
omitted explicit token ceilings required to admit one live session with deferred token usage.
The model had submitted one action, but physics executed zero; K1/Q/K2 never started.
This is a source-entry integration defect, not a model scientific outcome. Preserve that
entire root. Supply the same bounded single-session resource envelope used for C/12:
720 operations, 12 completed batches, 3,600 seconds, one provider session, 16M cumulative
input tokens, 4M uncached input tokens and 192K output tokens. No monetary price is invented.
Use a fresh v2 root for the pilot and full matrix, with unchanged P physics, questions,
resource card and interpretation. The completed provider-free acceptance is retained and
reused; fixed-recipe physics is unaffected. Validate deferred usage through the real ledger
before the next model call. All zero-action provider usage remains a separate prior cost.

Outputs: ignored raw roots with saved design, trajectories, provider receipts, numerical-tool
audits, token accounting, failed attempts and progress; English per-cell reports, aggregate
REPORT and machine summary with exact effective and additional-attempt counts. Retain pilot
and full matrix separately. No raw provider payload or credential is committed.

## Autonomous-path inventory correction (21 September, before full matrix)

The v2 integration pilot completed 12 batches, three posttests and one retest, with
exact replay, but failed inventory acceptance. Two causes were distinguished: the
audit assumed organic-phase sampling when the actual declared domain was reactor
liquid (audit-only error); transferring before an organic phase existed fabricated
product from the entire vessel, and reactor reconstruction could recopy vessel totals
(runtime defect). Preserve the v2 pilot and all its answers as invalid development data.

Opt-in P inventory v2 uses the existing finite-holdup transfer law on the actual reactor
liquid when no organic phase exists, retains other phases, and reconstructs reactor
inventory from its own phase rather than aggregate vessel contents. It retains the
original charge denominator. No scientific target, query, coverage or threshold changes.
Historical runtime remains available without opting in. Reconcile sampling against the
actual declared sample domain. Before new provider data, execute the failing pilot's
saved actions once as a 12-batch deterministic regression, plus its one retest, and rerun
the complete fixed qualification block (180 batches plus 5 aqueous probes) from unit one
under the corrected contract. Use those corrected reference observations as the sealed
60 Q targets and compare them with historical references. Then use a fresh study v3
root, a new separate pilot, and the unchanged 15-cell matrix. No invalid pilot is promoted.

The corrected block completed: 15/15 campaigns plus 5/5 aqueous probes, 185 batches,
3,110 operations and exact replay; 285.985 seconds. All fixed reference observations
and noiseless targets match the earlier gate exactly. The correction affects autonomous
prepartition operations, not query selection or favorable reference filtering.
Reproduce the corrected gate with `uv run --no-sync python -m scripts.run_work_ii_p_gate`
using `--design v3 --inventory-v2` and fresh `--root` / `--report` directories.
The new study root is `runs/development/work-ii-p-five-world-20260921-v3`;
its saved `inventory_contract` is `p-inventory-v2` for source, replay and retest.

## Parallel startup recovery (21 September, 00:33)

The corrected pilot passed all 12 source batches, three posttests and retest. The
15-worker burst then exposed host resource pressure: 11 lab MCP handshakes and one
local memory allocation failed before an accepted provider thread or physical action.
All twelve failures have empty provider stdout, zero operations and zero posttests;
the other three sources remain live and must not be interrupted or repeated.

Preserve each failed attempt and backfill only these verified zero-operation startup
failures into attempt-2. Bound each worker's BLAS/OpenMP threads to one and Tokio/Rayon
threads to two; stagger admission by five seconds while retaining a maximum of 15
simultaneous source workers. These are execution-resource changes, not changes to model,
questions, worlds, batch budgets or scientific criteria. Future identical local startup
failures share the existing six-attempt cap and bounded backoff. This exception does not
admit retries of scientific, schema, quota, authentication or source-budget failures.
The temporary recovery coordinator owns only those disjoint startup-failed cells; the
original controller continues its three live cells. It writes a separate liveness status
until the original controller exits, then produces one final combined export.

## Complete prepartition correction and fresh matrix

While the fixed v2 inventory-contract matrix was executing, W05/Aligned exposed the
analogous missing-receiver defect in concentration before extractant addition (step 9,
first batch). Preserve the entire running matrix and allow its fixed questions to finish;
do not hot-patch its physics or attribute this defect to the agent. Inventory v3 extends
the existing receiver semantics to prepartition drying and concentration, retains the
untouched phases and vessel volume, and updates reactor solute from its own operation
outputs. Washing still requires an actual organic phase, as already publicly stated.
No world parameters, question coordinates or pass thresholds change.

Before any replacement participant source, rerun the entire fixed 185-batch qualification
under inventory v3, test the observed concentration trajectory deterministically (12
batches plus its selected retest), and revalidate the successful technical pilot's exact
source and retest trajectories under v3. If the pilot is bit-identical, reuse its sealed
technical acceptance without another model call; preserve that explicit replay bridge.
Then execute a completely fresh 15-cell matrix in the v4 study root, using v3 inventory,
15 slots, bounded thread counts and staggered admission. The earlier matrix remains
separate development diagnostics, irrespective of which cells had passed; no favorable
cell is selected for replacement. All reference outputs are compared across revisions.
The final participant denominator remains 15; both whole-matrix costs remain visible.

The user subsequently selected six-way execution (fall back to four if needed). Existing
live sessions finish normally; the new matrix admits at most six cells, with the same
thread limits and staggered startup. Correct vessel volume must include every retained
phase after phase operations; the old pilot replay therefore changes valid action bounds
and is not bit-identical. It cannot serve as the new pilot: perform a fresh 12-batch
model pilot before the six-worker matrix. The first 13-batch concentration regression
remains recorded; the final volume-consistent correction gets a separate deterministic
13-batch regression and the complete qualification before new participant calls.

The final qualification completed before the new pilot: 15/15 fixed campaigns and
5/5 aqueous probes passed, totaling 185 batches and 3,110 operations with exact replay
in 398.453 seconds. All fixed reference observations and noiseless targets remain
identical to the historical v3 qualification. The final volume-consistent deterministic
regression also passed its twelve source batches and selected recommendation retest.
The current study root is `runs/development/work-ii-p-five-world-20260921-v4`, with
`p-inventory-v3` and six worker slots saved in its design. Its fresh pilot precedes
all fifteen participant cells. The entire earlier v3 matrix is retained as superseded
platform diagnostics: 180 batches, 45 posttests, fifteen executed retests, ten conforming
chains and five inventory-invalid chains, plus twelve zero-operation startup attempts.
None of its cells is reused in the current participant denominator.

## Pilot service interruption (21 September, 01:02)

The new pilot completed twelve batches, K1, Q and its recommendation retest with all
physical checks passing. K2 received HTTP 503 Service Unavailable before any answer or
calculator attempt. This is a transient provider-service failure during single-session
execution, not evidence of excessive concurrency or a scientific failure. Preserve the
original result and resume only K2 on its retained thread, carrying its calculator ledger
and counting the first rejected request toward the existing six-attempt maximum. The
P-local transport classifier now recognizes this explicit 503 event; quota and authorization
denials remain excluded. Physics, source actions, questions and numerical budgets are
unchanged. Only complete technical acceptance releases the six-worker participant matrix.

## Terminal disposition (21 September, 01:40)

The retained-thread K2 repair passed. The main matrix then completed at six-worker maximum
concurrency in 31.34 minutes, with zero source retries and no additional provider interruptions.
All fifteen sources used twelve batch starts: 180 starts, 178 final assays and two discards.
W05/Opaque discarded its first two vessels after phase-selection failures and obtained ten
final assays. Its replay, inventory and resources pass, but it remains nonconforming to the
twelve-final-assay specification. Do not replace it or add two experiments retrospectively.
Its submitted lifecycle-index-six recommendation was replayed faithfully; completed-assay
ordinal four is the same vessel, so no renumbering of the recommendation is performed.

All 45 posttests and fifteen recommendation retests completed. Fourteen chains meet every
check; all fifteen have valid prediction payloads and physically valid retests. Main summaries
retain all fifteen responses, with a separate both-conforming paired sensitivity analysis.
Aligned/Opaque/MisIndexed purity-eligible retests are 3/5, 0/5 and 0/5. This is the fixed
observed-assay threshold: W02/Opaque is close to it at 0.7992, and a single noisy retest is
not a repeated-probability estimate. Fixed targets, thresholds, source design and stop rules
were never altered to improve these outcomes. The final report separates all earlier invalid
blocks, pilot/service recovery, qualification and numerical-resource costs.

Final analysis corrected a floating-point boundary in the predeclared 0.02 direction
resolution. Six submitted pair differences mathematically equal to +/-0.02 were represented
slightly outside the boundary. A 1e-12 absolute roundoff tolerance now preserves the original
inclusive small-effect rule; it is not a changed scientific threshold. The initial exported
summary is preserved in the raw root. Only direction classification was recomputed; predictions,
MAEs, intervals, retests, qualification, source data and all denominators are unchanged.
