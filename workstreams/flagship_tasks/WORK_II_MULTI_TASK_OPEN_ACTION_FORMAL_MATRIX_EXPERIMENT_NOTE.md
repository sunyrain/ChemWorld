# Work II multi-task open-action fresh five-world matrix experiment note

Status: user-authorized fresh formal evidence block; frozen before provider execution.

## Question and fixed coverage

Does prior information arm affect autonomous experimental learning and transfer to a complete,
previously unevaluated ActionPlan across electrochemical conversion, reaction-to-crystallization,
and reaction-safety-constrained tasks? The matrix contains world seeds `0–4`, arms `opaque`,
`aligned_nominal`, and `misindexed_nominal`, one persistent 12-experiment session per cell, and
checkpoints after `0/3/6/9/12` experiments: `15` task-world clusters, `45` sessions, and `540`
participant experiments. All cells use DeepSeek V4 Flash and the fixed resource-recovery-v2 task
contract; this changes physical resource envelopes only where the prior qualification identified a
task-specific shortfall and does not limit provider calls.

## Measurements

Retain all scheduled cells, provider calls and failures, committed operations, physical-resource
ledger entries, belief checkpoints, terminal recommendations, selected candidate ranks, Top-1,
raw and normalized regret, final-law error, candidate/checkpoint truth executions, exact replay,
and public/truth/executed ActionPlan binding. Candidate outcomes and ranks remain hidden from the
participant. The scientific unit for arm comparison is the task-world cluster, not a provider turn
or individual physical experiment.

## Pass, failure, sampling, and stop rules

All `15` clusters and `45` sessions are fixed before provider execution; no seed, task, arm,
candidate, or unfavorable result may be replaced. Provider-free preparation must complete `240`
truth executions and `240` exact replays before the first provider call. The first scheduled
cluster (electrochemical conversion, seed `0`, all three arms) is an operational canary within the
formal denominator. After it completes, inspect representative trajectories, measurement use,
resource ledgers, provider receipts, checkpoints, terminal parsing, and replay. Scale to at most six
concurrent sessions only if no platform, execution-contract, resource-contract, provider-accounting,
or replay defect is found.

A scientifically valid negative outcome, poor rank, early participant stop, or resource exhaustion
under the declared contract remains in the denominator and does not authorize a favorable rerun.
A provider outage pauses pending cells and preserves completed cells. A defect that changed
participant-visible semantics, physics, action execution, stopping, or hidden-information exposure
holds the matrix; the smallest causally affected unit is rerun only under the frozen contamination
rule, with the original record retained. Interpretation-only defects are corrected downstream
without calling the provider again.

## Expected outputs

The ignored formal run root contains the frozen aggregate manifest, task-world configs and public
candidate packets, all raw cell records, progress, provider receipts, truth/replay reports, resource
ledgers, and a machine-readable aggregate summary with exact scheduled and retained denominators.
