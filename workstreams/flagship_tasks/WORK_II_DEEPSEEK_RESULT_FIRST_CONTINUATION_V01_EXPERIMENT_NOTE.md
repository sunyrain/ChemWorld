# Work II DeepSeek result-first continuation v0.1 experiment note

Status: frozen before the first continuation attempt; development evidence only.

## Question and tested units

Can the three remaining DeepSeek W2-26 task triplets complete under the unchanged scientific
design when provider token, call, currency and zero-action infrastructure-retry counts are measured
but never used to reject execution? The fixed continuation coverage is A-P electrochemical
conversion at 10 experiments plus A-S partition discovery and reaction-to-crystallization at 12
experiments: 3 triplets, 9 cells, 102 complete experiments and 45 typed checkpoints. Every triplet
retains world seed 0 and the `opaque`, `aligned_nominal` and `misindexed_nominal` arms.

The first six terminal triplets remain untouched. The two zero-action A-P electrochemical attempts
that ended before an accepted provider session also remain recorded; no completed trajectory is
replaced or reused.

## Measurements and execution rules

Retain all experiments, checkpoints, recommendations, attempted and committed operations, exact
replay, physical-resource ledgers, provider usage and every failure. Token, call and currency use
are descriptive only. Zero-action infrastructure failures may retry without a count ceiling, using
increasing backoff to avoid rapid relaunch. A retry is forbidden after any accepted scientific
operation; scientific or method failures remain terminal and are never replaced. Task, arm, seed,
round count, checkpoint plan, physical measurements and scientific stop rules remain unchanged.

## Expected outputs and claim boundary

Produce terminal raw reports for all three remaining triplets and one readable combined DeepSeek
observation summary across all retained terminal cells. This development cohort may identify
multi-task candidate patterns, but it is not formal Paper 2 evidence and does not authorize H1-H4,
provider ranking or release claims.
