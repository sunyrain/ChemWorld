# Work II：使用公开实际温度的 Astra 单次开发实验

2026-09-14，W2-100。用户授权修正合同并继续实验；仅GPT-6 Astra / medium，每条件一次。
本块问题、四格组件、学习/留出、物料、效用、32条E0正交覆盖、48条来源、12条盲查询、
48条Agent部署、24条参考部署、20次模型调用及全部阈值/停止规则，沿用
[前一工程块说明](WORK_II_ASTRA_CORRECTED_PILOT_NOTE.md)，仅做以下明确工程与观测修正：

1. 前块1条轨迹全部操作提交但温度检查失败，已关闭且不覆盖。新块复用该块私有初始化；不更换世界。
2. 加热温度是设定值，不能当作实际温度；不假设初温固定298.15K。
   在第8步冷却前，确定性执行器只读取最近公开`cool_crystallize`操作schema中的
   `current_temperature_K`，然后执行 `TC=275+f×(min(310,T_current)−275)`。
   这属于事前声明的状态解析控制配方；不能再称完全无反馈的开环温度流程。
   模型提前封存f与其余三控制，不根据HPLC结果改配方，不获得新的模型决策机会。
   实际温度/最终TC原样记录，所有模型及参考用同一执行器。
3. 公共来源记录包含实际冷却前温度和执行TC；盲查询只给设定值/f，不预先泄漏目标实际温度。
   支持诊断用实际温度，不能用设定值代替；组件参考从来源数据拟合实际温度后预测下游，
   全流程参考直接使用TR/tR/f/tC等控制。知识表中的温度也使用已公开的实际值。
4. E0除完整12步及重放外，核对第8步目标等于公开schema按f解析的温度，且未超过实际当前温度。
   动态解析只读取公开schema，不读history.info、私有状态或评价器真值。

固定分母164原始批次、20模型调用；前块1条工程失败另列，总授权尝试上限165原始批次。
来源各16批；每来源两种知识表达各一次，12个读出条件各一次。每调用600s、零provider重试、全块4小时。
失败、无效输出、中断及依赖未启动均保留，不用有利结果替换。单组开发试验不作系统性或正式论文结论。
新块输出独立的脱敏JSON/中文报告，原始轨迹/私有初始化留在ignored目录；完成后从current绑定访问。

## 已完成

164/164原始批次成功且精确重放；20/20预定fresh模型会话完成，零重试；4知识包、12读出条件均完成。
另保留前块1条温度检查失败及其通过的重放，不合并成当前块成功数据。
三来源留出查询近支持均0/6；历史最优配方复用接近Agent效用，无自然信息损失或系统性断裂结论。
详见[结果报告与图](reports/work-ii-astra-state-resolved-pilot-20260914.md)及同名JSON。
