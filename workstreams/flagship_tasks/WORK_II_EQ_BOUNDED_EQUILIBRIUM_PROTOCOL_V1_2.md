# Work II EQ bounded aqueous equilibrium protocol v1.2

This operational amendment inherits the scientific contract of v1.1 without change. The v1.1 provider-free gate remains authoritative because the worlds, priors, queries, thresholds, prompts, model, reasoning effort, budgets, denominators, scoring, truth embargo, and recovery rules are identical.

Two retained remote startup attempts occurred after the v1.1 freeze but before any provider process or scientific operation. The initial canary could not locate the installed Codex executable in a detached shell. Recovery attempt 01 reached Agent construction but could not create the source-instruction file because its private output parent was absent. Resource receipts record zero provider-process attempts and zero accepted model calls for both failures.

Version 1.2 makes exactly two operational corrections: the launcher supplies the existing authenticated Codex executable path, and the runner creates the private source-output directory before Agent construction. It creates a new immutable freeze manifest and uses recovery attempt 02 from the batch-1 boundary. All previous failure directories remain write-once audit evidence and do not enter the scientific denominator.
