# Work II A-S Study B3 runner-derived-status cross-model replication

Status: independent successor to terminal W2-57; provider-free preparation and both static checks
are complete. The user authorized the ordered DeepSeek-then-GPT canary block on 2026-08-31;
formal execution remains blocked pending a separate explicit authorization.

## Question and coverage

Does the W2-57 shared-index B3 comparison become operationally executable when the redundant
participant-written stage status is removed and stage identity is derived by the runner? The
scientific surface remains five public worlds, three prior arms, two fresh sessions per arm/world,
two turns per session, the same eight unseen scoring queries, and zero participant physical
experiments. Each provider has a three-session canary followed, only after both canaries qualify, by
30 formal sessions.

## Measurements

All structural, prediction, action, provider, continuity, usage, and failure measurements are
unchanged from W2-57. Participants still return the mechanism family, exponent, typed law,
predictions, confidence, public summaries, evidence assessment, and `selected_action_index=0..7`.
The runner owns pre/post stage identity; any participant `status` text is ignored and cannot affect
scientific scoring. Invalid scientific fields and action indices remain terminal schema outcomes.

## Pass, failure, and stop rules

Provider-free truth is reused only after exact provider-free-contract and source-manifest digest
validation. DeepSeek canary runs first; GPT canary remains unstarted unless DeepSeek completes all
three arms with two same-thread turns, 32 prediction terms, typed laws, and valid novel action
indices. Participant/schema/scientific failures are retained without replacement; only classified
infrastructure failures receive at most two retries. Formal execution requires a separate explicit
authorization field and cannot start from canary authorization alone. Once formal unit 1 starts, no
schema, runner-semantic, coverage, threshold, or denominator change is permitted.

## Expected outputs

Two independent prepared manifests referencing the frozen provider-free truth, static harness
checks, exact three-session canary records and closeouts, and, only after both canaries qualify and
formal is separately authorized, two immutable 30-session formal blocks plus a cross-model table.
