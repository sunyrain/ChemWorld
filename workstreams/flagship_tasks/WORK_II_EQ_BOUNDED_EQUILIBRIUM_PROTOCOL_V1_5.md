# Work II EQ bounded aqueous equilibrium protocol v1.5

This operational amendment inherits v1.4 and the complete v1.1 scientific contract. It changes no world, prior, query, threshold, prompt, model, reasoning effort, source or posttest resource, denominator, score, truth embargo, or scientific recovery rule.

The v1.4 W01 Opaque K1 recovery correctly reused the complete source thread and matched 128-attempt calculator policies. The provider produced no payload within the frozen 1200-second turn limit; the retained turn used zero calculator calls and no laboratory operation. This is a provider timeout eligible for the single authorized provider/transport retry.

Version 1.5 keeps the latest posttest session state while resolving the thread identifier from the durable complete-source receipt directory, then copies that private receipt into the new write-once attempt. Attempt 06 retries K1 once on the same source thread, with the same prompt, model, reasoning effort, schema, calculator budget, and timeout. It never reruns the twelve source batches and receives no truth or score.
