# Work II W2-62 Codex C2 full-cohort replication

Status: prospective development experiment; frozen before provider execution on 2026-09-02 and
authorized by the user's instruction to complete every participant-bearing main experiment with
both DeepSeek and Codex.

## Question and fixed coverage

Do the DeepSeek public-C2 findings on prior-conditioned search, belief revision, executable-law
formation and blind action persist for Codex `gpt-5.6-sol`/medium on the identical public scientific
surface? The block contains all `45` registered task-world clusters, three prior arms and `135`
fresh persistent sessions, with `1,260` planned complete participant experiments. It reuses the
same public worlds, prior packets, task contracts, resources, checkpoint schedules, physics,
evaluator queries and candidate ActionPlans as the terminal DeepSeek cohort. It does not reuse any
Codex participant context or trajectory.

The three W2-59 Codex C2 canary sessions remain immutable historical outcomes and are excluded.
This successor starts from its first cell in a new root and supplies a complete scheduled
failure-aware Codex denominator; it is not a continuation or replacement of W2-59. The first
electrochemical triplet is an in-denominator operational canary. Candidate outcomes, evaluator
truth and DeepSeek trajectories remain hidden from participants.

## Measurements and comparison

The production C2 runner and current-composite evaluator retain the original measurements:
experiment completion and resource use, checkpoint prediction error and calibration, supplied-
prior reliability, executable-law fidelity, blind action rank/regret, exact replay, provider usage,
thread identity and every failure. Primary reporting is model-stratified on all scheduled cells.
Cross-model estimates use matched task-world-prior rows and task-stratified task-world cluster
bootstrap intervals; prior arms are repeated factors. No pooled model leaderboard is authorized.

## Failure and stop rules

Participant, schema, scientific, resource and ordinary provider/session failures are terminal
outcomes with failure-aware scoring and do not remove later independent cells. A missing terminal
recommendation receives the existing C2 failure score. Only participant-visible contamination,
wrong physics/task/prior/resource semantics, source-plan drift, or a triplet-wide zero-operation
infrastructure failure pauses later calls. The existing bounded resume rule applies only to a
verified zero-operation infrastructure attempt; no completed or partially scientific trajectory
is retried or replaced. Coverage, prompts, thresholds, worlds and evaluator rules cannot change
after execution begins.

## Expected outputs

The ignored run root contains all `135` scheduled cell states, immutable raw/sanitized trajectories,
resource and provider ledgers, exact replay and a readable machine summary. A provider-free C2
current-composite evaluator then produces prediction, law and blind-action results for the complete
Codex cohort, followed by a matched DeepSeek-Codex report. Raw provider payloads and credentials do
not enter Git.
