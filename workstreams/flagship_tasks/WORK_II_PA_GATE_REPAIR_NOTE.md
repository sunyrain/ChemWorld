# W2-118：PA评估接口修复后的固定复测

2026-09-16，development，零provider，单executor。原五体系块已结束：PA的base/shift在动作前因评估器读取不存在的`env.compiled_mechanism`失败，三臂依规则记上游缺失。原23条件及全部失败保留，不覆盖。修复只将评价端机制引用改为`env.scenario_instance.compiled_mechanism`，不改变环境物理、公开信息、阈值或选择实验点。

问题：接口修复后，原PA四点响应、两相物料核算、世界分叉及局部三臂反证检查能否完成并通过？覆盖为base四批、shift四批、O/A/M各两批，共5个campaign、14批、98动作。操作、预算、S0v3分母、seed与噪声namespace、先验标定规则全部沿用[原固定说明](WORK_II_GATE_REPAIR_NOTE.md)的PA部分。先执行一项真实事务路径的回归测试，不将测试作为资格数据。

测量及判定不变：主对比1↔2与3↔4均超过`max(0.02,3√2σ)`；变化世界至少2个对应点超过该阈值；三臂公开入口送达、同操作物理/资源一致、A复测相容、M区间被排除。两相终检和0.90—1.05，初始目标库存与当前库存加采样损失误差≤1e-8mol。每campaign精确重放一次；非法事务终止该条件，缺标定则三臂记上游缺失，独立条件继续；不重试、不追加点、不调整阈值。

输出为独立`runs/development/work-ii-pa-gate-repair-20260916/`及`reports/work-ii-pa-gate-repair-20260916.{json,md}`，保存计划、输入、轨迹、核算与全部失败。原失败块与本复测块分开报告；最新逐体系状态允许引用此次PA结果，但累计尝试分母仍保留原5个PA条件。每20秒进度，预计1分钟内完成。
