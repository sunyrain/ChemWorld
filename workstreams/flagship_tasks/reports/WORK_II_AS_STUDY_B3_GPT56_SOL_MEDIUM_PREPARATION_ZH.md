# Work II A-S Study B3 GPT-5.6-sol medium provider-free 准备收束

状态：`provider_free_ready_participant_execution_blocked`。GPT participant calls 为 `0`，physical experiments 为 `0`；canary 与 formal session 均未启动。

## 已准备的冻结表面

- 5 个 development worlds；candidate-grid truth executions `1280`。
- exponent-grid 共评估 `520` 个 target，其中 `480` 个新 truth executions、`40` 个复用 target truth。
- 5 个 public worlds；public truth executions `160`；action-opportunity eligible `3/5`。
- input manifest 为 `30` cells = 5 worlds × 3 arms × 2 independent sessions；replicate blocks `10`。

## 与 DeepSeek v0.2 的等价性

| surface | exact scientific match |
|---|---:|
| frozen roster | True |
| qualification science fields | True |
| public worlds and truth | True |
| truth file roster | True |
| cluster packets | True |
| cell packets, scoring truth and hidden ranks | True |

## GPT harness

模型固定为 `gpt-5.6-sol`、reasoning effort `medium`；Responses transport、cached ChatGPT login、Codex CLI 与 disabled-tool command contract 均 ready=`True`。static provider calls=`0`。

## 执行边界

本工作包只授权 provider-free preparation。当前 participant execution 保持 blocked：不运行 canary，不运行 30-session formal block，也不把 DeepSeek canary 的单个 completed cell 作为跨模型科学结果。未来如单独授权 GPT participant execution，必须从这套冻结的 30-cell manifest 开始，并保留独立 provider receipts 与所有失败。
