# EQ 固定证据：第一对真实读出检查

日期：2026-09-21。身份：development，单条来源历史的两次独立读出。
执行说明：[实验 note](../../WORK_II_EQ_FIXED_EVIDENCE_EXPERIMENT_NOTE.md)；机器结果：[summary.json](summary.json)。

**运行链路通过，2/2 合法封存；这一对没有出现预期的先验收益/损害翻转。**
保留两份原始答案，不补跑、不改提示，不自动启动剩余 28 格。

## 实际执行与运行检查

使用固定队列第一条历史 H01（EQ-W01 原 Opaque 来源），R01 为无实例资料读出，
R02 为原定 Aligned 资料读出。两者的操作、原始谱、中间/最终测量、公开规则及 12 道预测题相同。
使用已准备的原包，没有额外实验、K1/K2 或模型调用重试。

| 项目 | Opaque | Aligned |
|---|---:|---:|
| 合法预测题 | 12/12 | 12/12 |
| 点预测及 80% 区间 | 36/36 | 36/36 |
| 模型阶段耗时 | 182.829 秒 | 234.922 秒 |
| 公共计算器调用 | 7/128 | 5/128 |
| 计算器成功 | 7/7 | 5/5 |
| provider 错误 / 重试 | 0 / 0 | 0 / 0 |
| 进程退出码 | 0 | 0 |

两个 thread 不同，实际模型均为 Sol、effort 均为 medium。保存的 prompt 与各自预定输入逐字一致，
两次 receipt、最终 payload、计算器审计和会话用量均已保存。工具监控未报告越界。
上下文配置为 872,000，实际记录的有效窗口为 828,400；未发现上下文压缩记录。
仅 R01/R02 有 started/terminal 文件，执行锁已正常释放，R03–R30 未启动。
零新模拟器执行，原来的来源实验和答案没有被覆盖。

## 预测结果：区分成功运行与科学方向

下列 MAE 对原五次参考观测的均值计算，三个响应等权；全部原始分响应、区间和计数在机器结果中。
这是一个世界中的一条历史，不能把题目、响应或参考噪声重复当独立世界。

| 量 | Opaque | Aligned | Aligned − Opaque |
|---|---:|---:|---:|
| 稀释三题宏平均 MAE | 0.209598 | 0.214364 | +0.004767 |
| 其余九题宏平均 MAE | 0.011275 | 0.014729 | +0.003453 |
| 全部十二题宏平均 MAE | 0.060856 | 0.064638 | +0.003782 |
| 稀释组三响应区间覆盖率 | 0/45（0%） | 0/45（0%） | 0 |
| 其余组三响应区间覆盖率 | 84/135（62.2%） | 72/135（53.3%） | −8.9 个百分点 |

预定交互差为 **+0.001313**。Aligned 在两个子组的宏平均误差均略高，因此不能把正交互差
写成“常规区域获益、稀释区域受损”。分响应也不一致：Aligned 的解离分数误差在两组均较低，
pH 和沉淀预测误差则较高。单对数据不支持五世界总体判断，亦不能证明两条件等效。

更突出的现象是**两个新读出都在稀释题上失准且区间覆盖为零**。两份最终公开 rationale 都说明采用
局部响应面外推；Opaque 强调原观测范围内响应变化较小，Aligned 表示用资料约束局部稀释方向。
它们虽称为极端条件放宽了区间，实际区间仍未覆盖原参考观测。这是科学表现，不能标作运行故障。
这些最终陈述不是内部推理过程的直接证据，也不足以把失败唯一归因于某种机制。

## 发现的实际代价与处理决定

完整原始谱使每次输入约 0.87 MB，实际后续上下文输入约 43 万 token。
工具往返重复提交上下文，因此累计计数远大于唯一输入的长度。

| CLI 报告用量 | 两格合计 |
|---|---:|
| 累计输入 token | 4,731,720 |
| 其中缓存输入 token | 3,435,264 |
| 非缓存输入（相减） | 1,296,456 |
| 输出 token | 11,776 |
| 其中 reasoning output token | 4,254 |
| 计算器调用 | 12 |
| 模型阶段总耗时 | 417.751 秒（约 6.96 分钟） |

这里是 CLI 用量，不是实际账单金额，reasoning output 是输出的子项，不重复相加。
当前大上下文设置有效；**输入成本高是本次发现的实际效率问题**，没有发现需修复后重跑的执行缺陷。
本次不根据较差预测去修改提示或追加样本。若以后改变输入表达或执行语义，必须清楚区分新旧条件，
不能静默替换这两格或把不同条件混成同一配对块。是否继续原队列由后续明确授权决定。

## 保留与复算

原始 provider 文件及凭据不进入此报告包。完整运行记录留在 ignored
`runs/development/eq-fixed-evidence-20260921-ready`，本报告的机器摘要不含 thread 标识或原始 provider 流。
可从两份已封存答案重新计算配对结果，命令不会再次调用模型：

```powershell
uv run --no-sync python -m scripts.run_work_ii_eq_fixed_evidence analyze-pair --root runs/development/eq-fixed-evidence-20260921-ready
```

本轮新增“两格后停止”入口通过 3 项相关定向测试及 Ruff；它复用先前已通过的真实 IO 准备流程。
这两格真实调用进一步验证了大输入、隔离会话、计算器、输出 schema、资源记录和评分路径。
