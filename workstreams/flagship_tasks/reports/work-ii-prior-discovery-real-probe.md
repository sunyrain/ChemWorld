# Work II prior discovery — real-provider probe

Status: **PASSED**

Scope: real provider, trajectory and interface qualification only; no scientific or benchmark claim.

The smallest real discovery probe completed **3/3 cells with 0 failures** on
electrochemical-conversion, world seed 0, across opaque, aligned nominal and misindexed nominal
prior conditions. Each cell completed five exploration experiments, four ordered snapshots
(pre-evidence, post-neutral, post-discriminating and final), two autonomous decisions, four
held-out queries with two replicates each, and three blind recommendation replicates.

The fixed provider was WellAU gpt-5.6-sol at medium reasoning over direct Responses requests.
Tools and MCP were disabled. The run used **18 calls / 18 attempts / 0 retries**, with **83,323
input tokens, 2,816 cache-hit tokens, 17,136 output tokens and 100,459 total tokens**. Provider
pricing was unavailable, so monetary accounting remains explicitly incomplete rather than zero.
Every trajectory passed local typed snapshot, executable law-summary, autonomous-plan, held-out and
blind-validation checks; raw provider payloads were not retained.

This probe authorizes the next development gate: one-seed breadth over all five tasks using the
same staged contract. It does not estimate any prior effect and must not be used to select tasks,
mapping permutations, horizons or hypotheses based on terminal outcomes.
