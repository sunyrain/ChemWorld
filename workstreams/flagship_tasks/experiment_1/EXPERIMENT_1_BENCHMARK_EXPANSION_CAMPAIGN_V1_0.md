# Experiment 1 benchmark expansion campaign v1.0

Status: **development-only expansion track; isolated from release/confirmation**

Baseline: `808d16e44111bfc5287ea4443b31b477a5dd0b13`.

This track expands the benchmark without changing the frozen release line, confirmation evidence,
Q1--Q8 thresholds, or existing failed-development records. No Participant execution is authorized.

## Ordered work

1. **C-S final redesign.** Preregister one genuinely new impurity-occlusion-law question, select or
   reject it on non-benchmark calibration Worlds, and stop permanently if it fails the frozen 3/3
   rule. Do not reuse seed-growth versus primary-nucleation.
2. **P first build.** Freeze five distinct executable Worlds, a task-specific extractant dossier and
   derangement, an executable S* oracle with matched bands, and a constant-K versus
   composition-coupled private family. Formal qualification remains closed until these assets are
   complete and hash-bound.
3. **D first build.** After P's asset pattern is reviewed, build five D Worlds, alpha* oracle/bands
   and a D-specific constant-alpha versus composition-dependent VLE fork under one public contract.
4. **FL v1.2 decision.** Do not repeat fixed-hardware work and do not reuse the failed reversible
   pathway fork. A new system-level question must be scientifically distinct and preregistered; if
   none is defensible, retain `0/15`.
5. **RX-E bounded review.** One limited review may test whether an entity-question redesign is
   justified. The existing C1/C2 permutation is not rerun as a repair; `3/5` is an acceptable final
   outcome.

## Evidence and resource rules

Each data-producing block receives one concise experiment note before execution. Outputs are
write-once, provider-free, exact-replayed and summarized with exact denominators and all failures.
Calibration cannot use formal World outcomes for selection. High-load FL/P/D campaigns are deferred
while the server is resource-constrained; focused tests and single-process calibration have priority
below the release track. New passing loci later receive their own challenge and confirmation; they
are never backfilled into earlier release evidence.
