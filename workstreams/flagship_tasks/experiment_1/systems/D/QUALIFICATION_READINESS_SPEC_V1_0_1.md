# Experiment 1 D qualification readiness specification v1.0.1

状态：**FROZEN FOR FAIL-CLOSED DEVELOPMENT AUDIT**

Participant：**未授权**

Formal benchmark：**未授权**

## 1. Scope

本版本审计 `D-W01..W05 × entity/parametric/structural` 在当前仓库状态下是否已经具备
进入正式 qualification denominator 的前置条件。每个 seed 运行一次
`reaction-to-distillation` 公开任务 smoke test，验证环境链路；smoke test 不替代 prior
discrimination，也不把五个普通 seed 认证为五套 private truth。

## 2. Frozen blockers

### D-E

仓库已有 generic reaction catalyst dossier 和历史 A-E 资产，但尚未冻结 Experiment 1 D 的五个
distinct executable World manifests、两个 reaction-duration anchors 与当前版本的独立证据。
因此当前只能确认环境/公共合同可达，不能把历史资产自动升级为本轮五世界 PASS。

### D-P

- 标准 feed、pressure、reflux context 下的 effective `alpha*` truth oracle 尚未冻结；
- aligned/misspecified 等宽 band generator 以及 early/later cut holdout 尚未实现为本轮合同；
- 因此 prior symmetry、可辨识性和预算内反证不能声明通过。

### D-S

- 当前正式 provider 主要执行 constant-relative-volatility shortcut；
- composition-dependent VLE 单元测试/通用热力学能力不等于已注册的 D-specific private family；
- 缺少同一公开合同下可切换、可 replay 的 constant-alpha 与 composition-dependent VLE fork；
- 因此结构层不得用 prompt 假装存在两套 world family。

## 3. Five-World rule

Seeds `0..4` 的 distillation relative-volatility multiplier 当前均为 nominal `1.0`。本审计只把
它们作为五个环境 smoke seeds。`D-W01..W05` 的正式 World manifests 与 `alpha*`/nonideality
参数必须在 repair 阶段由执行层冻结。

## 4. Gate interpretation

- Q1：失败；五个 distinct executable private Worlds 尚未冻结；
- Q2：由 smoke test 判断公开任务是否可执行；
- Q3：由 hash coverage、轨迹校验和 public-leakage audit 判断；
- Q4：D-E 可确认 dossier 形状资产存在，但整个 unit 仍因 Q1/Q5–Q8 失败；D-P/D-S 失败；
- Q5–Q8：失败；没有当前冻结版本的 discriminator、预算反证、行为后果和 noise evidence。

Provider calls 固定为 0。后续 repair 必须产生新版本与新 denominator，不能修改本轮 audit。
