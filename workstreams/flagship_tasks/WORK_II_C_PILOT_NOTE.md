# C-E crystallization pilot: execution note

Authorized by the user on 2026-09-19: run the proposed preliminary experiment. Development evidence only; this does not reopen Experiment 1 release qualification or change EC/PA results.

Preparation startup correction: v1 stopped during resource-card construction, before any environment action or provider call, because uncapped process time cannot also specify time-limit reservation allowances. Remove that unused reservation map; native physical time and costs remain in the environment ledger. Keep the startup failure in v1; execute the unchanged 19-reference design in `runs/development/work-ii-c-sol-pilot-20260919-v2`. No scientific result or query was selected or replaced.

Public-input correction: v2 stopped before any physical action/provider call because C's public catalogue still exposed named solvents, although its nominal dossier was anonymous. The C-specific catalogue and action labels now consistently use C/S codes. Preserve v2 public-input failure; restart the unchanged preparation block in `runs/development/work-ii-c-sol-pilot-20260919-v3`. Historical material replies are not rewritten.

Workflow correction before provider launch: v3 preserved seven reference operation attempts (six committed, one rejected seed operation), zero completed batches and a successful exact replay. The legacy v4 workflow requires current chemical assays before first seeding and filtration, contradicting this pilot's freely allocated 12 measurements. Use explicit `phase-resolved-process-v5`: identical v4 physical/seed-provenance laws, but those two measurement-order prerequisites are absent. Physical readiness, capacity, reaction advance, cooling bounds, crystal existence, filtration closeout and all resource limits remain. Legacy v4 is unchanged. Re-execute the complete fixed 19-reference block in `runs/development/work-ii-c-sol-pilot-20260919-v4`; questions, candidates and scientific thresholds are unchanged. This amendment precedes any provider source and no completed reference was replaced.

**Question.** Can GPT-5.6 Sol / medium discover a useful process under quality constraints and explain/predict material and trajectory effects? C-W01, latent crystallization materials, full-process v4; one goal, E Opaque/Aligned/MisIndexed, one attempt each. Only the S1/S3 property dossier is exchanged in MisIndexed.

**Fixed preparation block.** Before provider calls, save the exact actions generated by `scripts/run_work_ii_c_pilot.py`. Execute 12 prediction references as one 12-batch campaign; four feasibility references (the historical 12-segment seeded cooling pattern applied to S0-S3); and one identical instrument-check trajectory in each of O/A/M. Total 19 reference batches, plus exact replay separately counted. Check actual anonymous MCP material replies and dynamic instrument contracts. No agent may see reference outcomes. Retain every candidate, including infeasible candidates; no outcome-dependent selection of prediction recipes. Stop before provider launch on invalid actions, failed replay, incorrect public mapping, absent required instrumentation, or no feasible reference. Resolve a failed preparation block in a new version rather than overwriting it.

**Source budget.** Three independent sessions; 12 batch slots, 12 shared extra HPLC/particle measurements, 12 final assays and 720 operation attempts per source. Stock per source: reagent 0.48 mol, solvent 0.96 L, catalyst 0.06 mol, seed 12 g, matching twelve uses of public per-action upper ranges; actual consumption remains charged. No campaign process-time cap; physical time and cost are recorded. Source wall limit 3,600 s, finalization 300 s, provider attempts one, no automatic retries; token caps: 16M total input, 4M uncached input, 192k output. Free sequential/grouped design; no mandatory belief snapshots. Select one completed batch before K1. Native composite score is secondary.

**Endpoints.** Primary: maximize seed-excluded crystal recovery with crystal purity >=0.80 and fines number fraction <=0.50; recovery >=0.10 defines the reference feasibility target. Report all components, not just pass/fail. The public `crystal_size` is the bounded index min(d50 / 250 micrometres, 1), not an uncensored physical diameter. All four Q targets use public 0-1 metric definitions; predict noise-free process values before the final assay's sampling, with 80% intervals. No-particle size is marked undefined in evaluation, and particle-presence predictions are retained.

**Posttests.** Same original thread: K1 free mechanism explanation; Q twelve saved recipes spanning the six preselected contrasts; K2 three retrospective questions. Each stage has 128 public calculator attempts and a 1,200 s response deadline. Exhaustion rejects computation but permits answer completion. No truth feedback or new laboratory actions. Mechanism prose is retained for later grounded assessment; no additional LLM judge calls in this pilot. Per-metric errors, interval coverage/width, paired direction and quality decisions are reported; pair effects within 0.03 are scored as ties. The 0.03 resolution is descriptive and not a qualification gate.

The 12 query slots contain nine distinct recipes: Q01/Q03/Q05 share one control, and Q07/Q10 share another. These are dependent paired comparisons, not twelve independent scientific samples. Report both the slot denominator and the number of unique recipes; the pilot's independent source denominator remains three sessions in one world.

**Reproducibility and failure.** Exact replay of each source and reference. One independent observation-noise retest of each valid selected recorded batch (three planned), with the original action sequence including sampling; it does not establish stochastic process robustness. Scientific low performance never triggers retries or changed thresholds. Keep partial sources and attempt posttests if terminal context is intact. Input/replay defects stop subsequent sources; an individual provider/posttest failure remains explicit and does not erase completed sources. No provider recovery is automatically scheduled in this new block. Preserve paid source sessions in an ignored neutral runtime workspace for possible separately recorded infrastructure recovery.

**Denominators/output.** Three planned sources, 36 planned source batches, nine follow-up stages; 19 preparation batches and three recommendation retests counted separately, plus all replay operations. Planned versus completed versus valid must remain distinct. Raw data under `runs/development/work-ii-c-sol-pilot-20260919-v1`; English readable report and safe machine summary under `workstreams/flagship_tasks/reports/work-ii-c-sol-pilot-20260919/`. A 30-second heartbeat exposes stage, operations, completed batches and elapsed time. Single executor. No manuscript claim is inferred from a pilot's single world.

## Authorized repair, 2026-09-20

Question: does the selected MisIndexed batch reproduce when its recipe contains only physically committed operations? The original extractor included rejected duplicate seeding attempts, causing the strict retest to stop. Preserve the full source trajectory, sealed recommendation, all nine posttests and the failed retest. Execute exactly one corrected MisIndexed recommendation retest, with the same selected batch, world, observation seed 303, sampling and resource envelope. Remove only transactions explicitly recorded as rejected; do not change successful operations. Require one terminal assay, legal execution and exact replay; record all four scientific metrics regardless of quality or recovery. No provider calls or new optimization. Store the repair separately and distinguish original versus repaired chain completion and total retest attempts. A scientific failure remains a result, not a retry trigger.

Also correct the report's misleading use of quality-only `Feasible`: quality and recovery >=0.10 are separate conditions. The original observations and outcome definitions remain unchanged.

Repair outcome: one additional provider-free retest completed, with nine committed operations and exact replay; 40 rejected seeding attempts were excluded and retained in provenance. Net recovery is 0, purity 1 and fines 0: quality passes but the recovery target fails. Original source, recommendation/posttest result, trajectory, provider receipt and failed retest were checked unchanged. The effective pilot now has 3/3 complete chains, 36/36 source batches and 9/9 posttests, with 2/3 original complete chains and four total recommendation-retest attempts (one original failure retained). No model session was rerun.
