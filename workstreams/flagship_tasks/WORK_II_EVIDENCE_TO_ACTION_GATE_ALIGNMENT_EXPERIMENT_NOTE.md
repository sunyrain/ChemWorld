# Work II evidence-to-action gate-alignment analysis

Status: frozen retrospective provider-free analysis; no new world execution and no oracle redesign.

## Question and fixed coverage

Does the frozen `rho >= 0.80` complete-ranking oracle gate track the action endpoint that the planned
causal study actually treats as primary? Analyze every completed unit from three retained blocks:
the `8` attempted W2-51 96-grid fresh formal-preparation clusters, the `7` W2-52 320-grid exposed
construction units, and the `1` W2-52 320-grid fresh prospective qualification cluster. The resulting
`16` unit-version rows are fixed. Repeated task-worlds across oracle versions remain explicit paired
route comparisons and are not treated as independent worlds.

## Measurements

Recompute the executable law-implied ordering from each retained typed law and its frozen eight
candidate feature rows. Join only afterward to the retained candidate truth. Report Spearman rank
correlation, selected truth rank, Top-1, selection within `0.01` of the best, raw and normalized
regret, and pairwise ordering agreement after excluding truth gaps below `0.01`. Summarize these
readouts by evidence role and report the four exposed 96-to-320 historical-failure repairs separately.

## Integrity and interpretation rules

All `16/16` rows must reproduce the recorded Spearman and Top-1 values, retain zero fit/candidate
overlap, and prove that the fitter did not read candidate outcomes. Missing or mismatched rows fail
the analysis rather than being dropped. W2-51 and W2-52 dispositions, seeds, `rho >= 0.80` gate and
stop rules remain unchanged. Exposed construction rows are diagnostic only; the single 320-grid
prospective row cannot establish a generalization rate. This analysis can diagnose whether complete
ranking and action sufficiency separate, but it cannot retroactively authorize provider execution.

## Expected outputs

One machine-readable summary with exact denominators and every row, one reader-facing Chinese
closeout, and one paper-story update. Provider calls, physical experiments and new truth executions
must all remain zero.
