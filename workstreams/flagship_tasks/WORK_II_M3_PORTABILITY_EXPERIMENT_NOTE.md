# M3 information separation and context portability

Design fixed before new candidate outcomes or recipient calls on 2026-09-05.
The user's continuation authorizes this next block. W2-69, single executor on main;
development qualification first, then one source freeze and one formal execution.

**Question.** Does a sealed model-generated quadratic, without its source dialogue or
raw observations, improve a fresh recipient's new decisions relative to task information
alone? Compare none / raw evidence / L-only / F-only. This is same-world context portability
on new candidate plans, not new-parameter, topology transfer, internal mediation or a claim
about scientific discovery independent of numerical tool access.

**Fixed coverage.** Reuse all ten M1 physical worlds, all twelve public observations/world
and all forty sealed model/repeat source artifacts through `configs/current.json`'s M1
binding. No selection by M1 success, error or effect. Private mechanisms, initial-state
construction, objective, control ranges and fixed controls remain identical. Eight new
candidates/world use the existing outcome-blind 8-point LHS generator with seed 90608;
the same normalized coordinates are used across worlds and are disjoint from M1 evidence
and candidates. Candidate truth uses keyed noise seed base 90600 and the M3/world namespace.
There are 80 new hidden virtual experiments plus 80 exact replays, zero new source
measurements and zero recipient measurements. The independent-world denominator remains
ten, five/task; forty sources and 160 fresh recipient sessions are nested, not new worlds.

**Information and execution.** All groups get the same task, utility direction/range,
coordinate/basis explanation, complete candidate controls/ActionPlans and minimal ID schema.
Only raw receives the twelve public observations (including complete plans); only L/F
receives six coefficients and their common approximation semantics, without provenance
labels. None receives neither. No arm sees source dialogue, world identity, previous
candidates or hidden candidate outcomes. L and F are reused exactly, without refitting.
Recipients are tool-free and use the M1 DeepSeek-v4-flash/high and GPT-5.6-sol/medium
configurations, two repeats/model/world. This matches recipient tool permissions; it does
not remove the original model-versus-numerical-solver computation difference at source.
No word-count padding: information content and context length are part of the deployment
strategy. Report actual prompt bytes and provider input/cache/output/reasoning tokens.

Permute the forty source states with NumPy seed 20260907. Within each world assign the
four nested model/repeat states cyclic rotations of [none,raw,L,F] using offset
(2*model_index + repeat_index + world_index + task_index) modulo 4. Each arm occupies each
serial position once/world. All sessions are fresh; no repair turns or paid retries;
600 seconds/session, requested 2,048 output tokens (not a reasoning-token hard cap).
Planning: about 49 provider minutes plus 12 physics/replay minutes at M1 decision rates;
the sequential timeout envelope is 26 hours 40 minutes. No currency estimate.

**Readouts and fixed decision rules.** Primary is L minus none failure-aware regret,
averaged over four nested states/world, equally over worlds/task and over tasks. Utility
scale is 1; missing/invalid choice receives regret 1. Material improvement requires the
upper endpoint of the two-sided 95% interval below -0.01. Five secondary contrasts are
raw-none, F-none, L-raw, F-raw and F-L, with 99% marginal intervals (Bonferroni).
Bootstrap 20,000 times, seed 20260908, resampling five worlds within each task and preserving
nested pairs. Intervals are approximate; sampled worlds were already used in M1.
Show every world, task means, raw/completed-only losses and their denominators, Top-1,
regret <=0.01 near-optimal rates, missingness and L/F agreement with exact maximization.
Report deterministic L/F maximizers, nearest-public-observation and uniform-random expected
regret on the new candidates as descriptive controls, without new provider/physics counts.
No noninferiority, equivalence or experimental-savings claim from a nonsignificant contrast.

**Failures and preservation.** Retain all 160 opportunities, all 80 physical opportunities,
all failures and all costs. Resume sealed receipts; a started call without receipt becomes
an interrupted failure and is never relaunched. Missing L/F blocks only its dependent arm.
Unknown IDs, extra fields and invalid JSON are participant failures, with no repair.
Physical/ActionPlan/replay failure stops remaining dependent work; forbidden tool activity,
missing/reused session identity or information leakage stops the block and invalidates formal
inference. No effect-based stopping, extra seeds, favorable replacements or design changes.
Seal all recipient and deterministic choices before analysis loads new hidden outcomes.
An execution-semantic platform fix requires a new complete affected block; keep the old one.

**Qualification and outputs.** Provider-free fixtures test all four actual outgoing prompts,
retained failure/resume behavior, exact schedule/denominators and world-level inference;
two seed-0 candidate executions (one/task) test shared physical/replay semantics outside
the formal denominator. Synthetic outcomes are test fixtures, never paper evidence.
Runner `scripts/run_work_ii_m3_portability.py`; protocol
`configs/benchmark/work_ii_m3_portability_20260905.json`; ignored raw output
`runs/work-ii-m3-portability-20260905`. Export one sanitized JSON/Markdown summary with
choices, public inputs, original coefficients, post-seal scores, intervals, failures and
resources. Account separately for new deployment/evaluation costs, reused M1 public
experiments and original source-generation costs; reused costs are not incurred again.
Update the existing TODO/matrix/results and paired-world figures regardless of effect sign.
