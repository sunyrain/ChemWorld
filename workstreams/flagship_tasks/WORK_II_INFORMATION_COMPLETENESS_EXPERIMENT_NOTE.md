# Work II：公开观测映射完整性的单因素干预

2026-09-06；任务W2-87。用户已授权优先尝试本方向并先完成GPT。A/B/C开发资格已封存，
GPT独立冻结的正式块60/60完成；用户于2026-09-07要求关闭DeepSeek推理，low正式块已停止并保留，none开发已终态但未取得正式资格。旧正式证据与论文保持原终态；
第三模型不进入本块。执行状态由[TODO](WORK_II_TODOLIST.md)管理。

## 当前终态：关闭推理已实现，none未通过正式资格

[标准头透传开发报告](reports/work-ii-information-deepseek-none-transport-development-20260907.md)：
固定6/6会话尝试、0完整通过；共尝试10/12轮，另外2个post因pre失败未启动。
3个结构化提交失败（1 pre、2 post）、3个provider输出上限中断，全部保留。
7/10轮usage可用，7份均显式reasoning=0；3轮缺失usage不能补作0。
21次上游请求均记录effort=none、HTTP200，无透传连接失败或额外重试；
6个fresh thread、4个实际pre/post续接一致；科学输入与原开发6单位完全一致。
15次计算工具尝试中4次表达式拒绝，本地计算0.0041695秒；wall合计377.328秒（6.289分钟）。
去重后可用usage为input206789（cache164096）、output22146、reasoning0，缺失部分为下界。

关闭API thinking并未阻止模型把分析写入普通输出；出现JSON外说明及达到max_output_tokens的中断。
这是当前harness配置的提交/资源资格失败，不能写作none的科学恢复率结论，也不能与GPT合并主效应。
按预先规定停止进入60正式：预算仍为空，无运行中任务、无自动追加或重跑；正式ETA不适用。
low（2完成、1工具预算失败、1用户中断、56未启动）及两次none配置失败开发（各1尝试、5未启动）
均由current单独绑定和保留。GPT仍是W2-87唯一完整正式结果，稿件/PDF整合另行处理。
交付验证：43项定向测试、Ruff与diff检查通过；包括HTTP body/SSE/错误原样转发、
停止时关闭上游连接、零额外重试、none非零或缺失usage时阻止post，以及累计usage去重。
当前文档72处本地链接有效，current报告绑定与科学输入核对通过；没有重跑历史正式证据。

## 用户要求关闭推理：独立none块（2026-09-07，执行前固定）

用户在核对延迟后明确要求不要推理，选择依据为运行成本偏好，不是科学作答得分。
停止原Flash/low正式块：计划60，已完成2、工具预算失败1、用户中断1、未启动56；
原始输入、冻结来源、作答、失败与中断记录保留，未启动部分不续接为none，也不并入新正式效应。
none仍为deepseek-v4-flash及官方Responses入口，reasoning.effort固定none；仅关闭thinking，
GPT已完成的medium配置和结果不变。新块是披露效应的配置复制，不声称跨模型等算力。

问题：无推理配置能否在相同harness中完成结构化pre/post并保持会话续接，耗时是否可接受？
开发覆盖仍为既有独立开发world×3先验×2信息条件＝6会话/12轮，顺序、公开输入、工具与评分不变。
每轮600秒、会话1200秒、整块7200秒、provider重试0，post计算工具最多8次尝试；
保留所有失败和资源消耗，沿用provider/schema失败继续、平台/越界工具失败停止的规则。
先用真实Codex对本地回环服务验证none逐字进入请求及零隐藏重试，新增真实provider调用0。
技术通过须6/6结构有效、12/12收据与usage完整、6个fresh thread和组内一致续接；
12轮reasoning_output_tokens显式存在且为0，同时确认本地实际请求发送none。
不要求采用工具或科学恢复成功；工具表达式拒绝消耗预算且保留。开发不通过则停止进入正式，
不换world、阈值或重跑失败单元取得通过。输出独立机器/可读报告及ignored原始收据。

通过后用实测耗时固定none正式总预算，再冻结同十world×3先验×2信息条件＝60新会话，
沿用已固定的主读出、每轮/会话工具与时间限制、零重试及world bootstrap；不重做物理资格。
科学输入与GPT正式逐字比较，low停止原因和全部60分母单列。未取得none开发结果前不承诺ETA。
usage汇总按同thread最后可用累计值去重，中断缺失计下界；这是已验证的分析口径修复，
不改变任何模型输入、评分或执行行为，旧GPT/low原报告保留并另行导出资源修订版。

### none入口配置失败与两项工程定位（真实探针前固定）

none开发首个pre回执耗时363.828秒、reasoning43979；actual turn context为none，
本地同版本Codex请求也已验证none，但真实回执不满足关闭推理的资格要求。
按平台配置不匹配停止后续单位：1会话已尝试且post中断、5未启动，原始pre作答和全部记录保留；
不进入正式、不把它写为none科学效果或用重跑挑选有效答案。

独立工程问题：是DeepSeek原生接口未执行关闭开关，还是Codex请求映射产生偏差？
固定两个顺序工程探针，均仅请求公开常量JSON {"ok":true}，不读取world、先验、评分或私有种子：
1. 官方Responses接口，reasoning.effort=none，max_output_tokens=128。
2. 官方Chat Completions接口，thinking.type=disabled，max_tokens=128。
每请求60秒、全块120秒、重试0；记录请求开关、HTTP结果、响应状态、usage/推理token、
文本是否有效、wall和全部失败。关闭成功要求显式报告reasoning=0且返回有效JSON；
usage缺失为未知，不能当0。每30秒提供进度。输出一个机器/可读工程摘要，原始响应仅存ignored目录。
这些是接口工程探针，不是新科学单元，不进入GPT/DeepSeek正式分母。
依据两个开关的直接结果定位下一步；尚未授权其结果为科学证据，none正式继续等待。

两探针终态：Responses none在0.391秒返回有效JSON且reasoning=0，通过；Chat disabled在0.516秒
返回有效JSON且无reasoning_content，但usage未显式给reasoning计数，按预定规则记未知/未通过。
原生Responses能够关闭thinking，因此追加一个独立工程定位请求（执行前固定）：
同版本真实Codex、同none配置、同结构化输出模式，仅请求公开常量JSON；通过本地回环转发器
原样转发一次到官方Responses，记录实际出站reasoning参数、响应usage和wall，原始请求/响应ignored保存。
单请求60秒、零重试，不提供world或评分、不改写上游请求参数；有明确非零reasoning或映射差异则定位为未解决。
这项探针不以生成内容正确以外的任何科学成绩选配置，也不计入正式实验。

回环转发定位终态：真实Codex出站body明确为reasoning.effort=none，2.218秒、output5、reasoning0。
转发器保留body但只发送标准Authorization/Content-Type头；与原生直连还存在请求头差异，
因此尚不能把原失败简单归为body映射错误。下一工程块固定复用此公开常量body，顺序做两请求：
完整Codex头（去掉Host/Content-Length，由HTTP库重算）及只添加Codex User-Agent的标准头。
两请求各60秒、零重试，记录响应usage、wall和头名称；不发布认证值或客户端标识。
先封存两条件，再执行；无world、私有评分或科学结果选择。判断标准仍为有效常量JSON及显式reasoning=0。

头对照终态：完整Codex头及仅Codex User-Agent均返回有效JSON，但reasoning分别49、15；
相同body的标准头转发为0。当前入口对Codex User-Agent的处理使none失效，不能只凭配置或body宣称无推理。
平台修复仅为本实验provider显式配置User-Agent=ChemWorld/0.2，保持官方Responses直连、模型、none、
所有科学输入/工具/schema/预算不变；不部署转发器。真实Codex回环测试须确认此头与none同时出站。
受影响开发资格从首单位另行执行：同6会话/12轮、相同覆盖、测量、预算、通过和停止规则，
原none失败块及low全部保留。新输出根为work-ii-information-deepseek-none-fixed-20260907；
须12轮显式reasoning=0且全部技术通过，才固定和启动none正式60会话。

仅覆盖User-Agent的重资格同样未通过：首pre为160.410秒、reasoning20483；停止其post和后续单位，
1会话尝试、5未启动全部保留。User-Agent是已证明的一个触发条件，但不能据此断定它是唯一条件。
下一工程块固定两个短请求，仍复用公开常量body和已捕获的Codex头：
（a）只将User-Agent改为ChemWorld/0.2；（b）同时将Originator改为ChemWorld。
每请求60秒、零重试，沿用显式reasoning=0及有效JSON的标准；完整记录两条件和全部失败，
无科学数据。确认完整原生请求头控制有效之后才再开启科学开发资格。

两项补充头对照仍未关闭推理（reasoning21/135），仅覆盖UA/Originator不足。
下一修复采用已验证可关闭thinking的标准HTTP头透传：本地回环仅原样转发官方Responses请求体及
Authorization、Content-Type、Accept、必要Content-Encoding，使用ChemWorld User-Agent，
不转发Codex身份/元数据头。没有Responses→Chat转换、不改prompt/schema/tool messages；
仍由真实Codex执行并续接，原始输入和评分不变。透传生命周期归同一会话执行器所有，
退出/超时关闭上游socket，零额外重试；记录每个请求的起止、请求模式、HTTP状态和取消。
先验证原样body/标准头、SSE与HTTP错误保留及取消，再重新从首单位运行同6会话资格，
输出根为work-ii-information-deepseek-none-transport-20260907。每轮回执若未显式报告reasoning=0，
立即按平台配置不匹配停止后续单位；此检查落实既有无推理技术通过条件，避免再消费post请求。
其余覆盖、测量、预算、科学通过/失败定义和输出均沿用原none开发说明，前两失败块完整保留。

## DeepSeek执行前补充：Flash低推理配置

2026-09-06，用户授权推进DeepSeek并优先考虑Flash的速度与比较公平性；在DeepSeek新块首单位
之前固定为`deepseek-v4-flash / low`，使用官方原生Responses入口及现有Codex harness。
旧配置已是Flash/high，GPT为GPT-5.6-sol/medium且报告了reasoning tokens；Flash不是关闭
thinking的同义词。[DeepSeek官方说明](https://api-docs.deepseek.com/guides/thinking_mode)规定
默认high，medium/xhigh均映射high，low保持low，Responses的none关闭thinking。
[Codex配置说明](https://developers.openai.com/codex/config-reference)将reasoning effort作为
模型设置；同名档位不提供跨提供方计算量相等的保证。本次按速度取向选low，不追加high/none
选优实验，不根据恢复结果选择档位。历史high及GPT正式结果均不替换。

固定开发块仍为既有独立开发world×3先验×2信息条件＝6会话/12轮；沿用原证据、完整合同、
schema、工具、评分和顺序，不产生新物理数据。每轮600秒、会话1200秒、整块7200秒，
provider重试0；所有失败保留，provider/schema失败继续下一单位，平台/越界工具失败停止。
技术通过要求6/6结构有效、12/12收据与usage完整、fresh thread及pre/post续接正确，
配置/实际harness支持low；不要求工具采用，也不以科学恢复率判定技术通过。
输出独立开发机器JSON/可读摘要及全部原始收据（ignored目录）。

开发通过后固定正式总时限并执行同一十world的60会话/120轮；每轮600秒、会话1200秒及
零重试与GPT一致。正式前绑定模型目录这一实际执行依赖；仅冻结新DeepSeek执行面一次，
不刷新已完成GPT的历史资格。正式主分析、保留失败、world bootstrap和停止规则沿用原设计。
公平性含义是相同公开证据/任务/工具机会及每个模型内部相同资源规则；分别报告模型配置、
耗时、输入/输出/推理用量。跨模型合并估计两个明确配置的平均披露效应，不作等算力排行榜。

### DeepSeek开发终态与正式冻结预算

[开发报告](reports/work-ii-information-deepseek-low-development-20260906.md)及机器JSON已封存：
6/6会话有效、12/12轮收据和usage完整，6个fresh thread，组内续接正确；3对实际提示词
只在post完整合同披露上不同。会话wall合计2410.343秒＝40.172分钟；单会话244.672–532.359秒，
单轮89.641–442.469秒。没有provider/schema/platform会话失败或重试。
27次工具尝试含21成功、6次模型表达式SyntaxError拒绝，均消耗工具预算，原记录全部保留。
技术通过不要求工具零拒绝；没有平台修复或更改接受条件。

原汇总将各轮CLI usage相加，记录输入1987018 tokens（cache1779456）、输出441242（reasoning423602）；
续接usage为会话累计值，该加总重复计入pre，去重资源口径见下方延迟核对。原收据与历史报告保留。
真实Codex对本地回环服务的4项定向检查确认high/low值逐字进入Responses请求，HTTP/SSE失败
均只发送一次；没有调用真实provider或增加科学样本。low仍包含大量推理，不能声称关闭推理、
等算力或已经因降档获得速度收益。正式依据本次完整校准外推24103.43秒＝6.695小时，
固定软ETA 6–8小时；整块硬预算43200秒（12小时），每轮600秒/会话1200秒/重试0。
总窗口用于顺序执行相同60单位，不把更长总窗口解释为每次决策得到更高预算。

开发恢复为原说明0/2、完整合同2/2，正确先验保持两组各1/1；仅为开发记录，
不进入正式分母，不据此改变low配置、world、阈值、条件顺序或停止规则。

### 运行中延迟核对（2026-09-06 23:57 +08:00）

只读核对现有收据和运行中的配置；新增真实provider调用0，未修改冻结执行面或正在运行的实验。
相同6个开发会话，GPT/medium与Flash/low平均每会话分别134.318秒和401.724秒，约3倍；
pre平均30.516/132.174秒，post平均103.771/269.511秒。Flash的pre未调用计算工具仍明显较慢。
Flash本地工具计算合计0.012991秒，轮外编排合计0.234秒；主要时间位于模型调用阶段。
Flash共27次计算工具尝试（6次表达式拒绝），GPT为0；工具后的模型续推增加往返，
部分会话另有planning事件。无已记录的provider错误或重试；现有日志不能把服务端排队、
首token等待与逐token生成精确拆开，不能据此排除服务端排队或限速。

本地回环服务向真实Codex顺序返回两轮已知usage：input 100/200、output 10/20、reasoning 3/6；
CLI收据为100/300、10/30、3/9，确认post为会话累计值；两次请求均传递reasoning.effort=low。
同thread完整会话以最后累计usage计总量，post增量为post减pre；中断会话保留最后可用累计量并标为下界。
按此口径，Flash开发输出337127（reasoning322943，95.79%），GPT开发输出33707（reasoning28965）；
两模型tokenizer不同，不能据此当作等算力比较。原加总偏差仅影响资源解释，不影响wall、科学作答或评分。
终态分析须从原始收据重建去重资源摘要，保留原报告，不重跑科学单元。

正式已终态2/60（1完成、1工具预算失败），第3会话运行中。第2会话pre实测290.063秒、
推理37997 tokens，实际turn context为Flash/low，未调用计算工具。前2个终态会话速率外推
约07:05结束，样本很少；仍采用开发校准的6–8小时总软窗口，硬截止2026-09-07 11:39 +08:00。
运行中的low不更改；none关闭thinking的速度和有效性尚未经本块校准。
正式覆盖和主分析沿用原十world设计。GPT、DeepSeek分别终态后，再按同world内两模型平均
合并；每个world仍是一个重采样单位。两个配置的资源及效果分开报告。

## GPT正式终态（2026-09-06）

以下保留GPT阶段终态；DeepSeek最新执行设计见上文补充，实时阶段由TODO和运行摘要管理。

[机器报告](reports/work-ii-information-gpt-formal-20260906.json)、
[可读摘要](reports/work-ii-information-gpt-formal-20260906.md)及
[独立图件](../../paper/figures/prior-discovery/figure-10-information-gpt.pdf)已生成。
GPT-5.6-sol/medium完成60/60会话、120/120轮，零provider/schema/platform失败、零重试，
全部轮次usage完整。正式北京时间20:06:14–22:25:31；会话wall合计8355.718秒（2.321小时），
加6会话开发合计2.545小时。开发结果独立保留，不进入正式分母。

| 正式读出 | 原公式说明 | 完整观测映射 |
| --- | --- | --- |
| 未知先验联合恢复 | 2/10 | 10/10 |
| 错误先验联合恢复 | 4/10 | 10/10 |
| 主分析：未知/错误先验联合恢复 | 6/20 | 20/20 |
| 正确先验保持（单独报告） | 8/10 | 10/10 |
| 主分析：平均指数绝对误差 | 0.3525 | 0.0000 |
| 主分析：四指标留出预测MAE | 0.0387092 | 0.0060550 |
| 主分析：平均归一化决策regret | 0.0709737 | 0.0074811 |
| 主分析：留出候选Top-1 | 5/20 | 12/20 |
| 数值工具采用（全部先验） | 0/30 | 0/30 |

主效应完整−原说明为+0.70，预定world-bootstrap近似95%区间[+0.45,+0.95]；
10个world中8个提高、2个持平、0个下降。逐原始终答独立复算60个恢复判定、预测MAE和
20,000次world区间均一致。正确先验保持不混入40会话主分母。

终态核对：60个独立thread，每会话pre/post同thread；30对pre提示词逐字相同，
post除新增完整合同外逐字段相同。完整组post平均13144.8字符，原组10737.5字符；
信息长度属于处理。7个冻结执行文件及封存输入均未改变，实际交付的完整合同由公开初温
生成，不接受目标指数。原报告将pre累计usage重复计入；2026-09-07按每会话最终累计值修正为
输入1552862（cache332544）、输出327180（reasoning278923），120/120有usage；工具计算与重试均0。
[资源修订报告](reports/work-ii-information-gpt-formal-resource-corrected-20260907.md)已绑定current；
原报告与全部收据保留，rows、groups、primary、world contrasts及分母逐项一致，未重新调用模型。

该结果支持本GPT配置、固定候选与同族参数覆盖下的披露效应：公开终点映射完整性是一个
可干预的成功条件。它不等于所有Agent的共性，也不隔离合同中的某个公式、纯语义或内部
推理机制。完整组结构恢复20/20仍只有12/20精确Top-1，结构恢复与行动选择继续分开报告。
参考/目标的配对噪声、有限候选和固定坐标是本实验范围，不宣称新机制或新条件迁移。

本次两组共同使用明确的v3合同；B1的24个映射失败和旧B3的v1合同差异保留，
不能用本次成功回填历史公开可识别性或将历史失败归为同一种原因。现有稿件/PDF尚未整合本块。
GPT剩余会话0；后续仅DeepSeek 6开发+60正式。其正式旧协议折算约7.58小时，
必须先完成新协议开发校准再固定预算，不能当作实测交付ETA。

## 执行前设计与阶段记录

阶段A终态：96/96尝试全部通过，kernel最大绝对偏差3.33e-16，条件性幂律反解最大指数误差2.00e-15；
新增provider/完整物理/replay/独立world均0。约0.016秒的计时受Windows时钟分辨率影响，
不作为吞吐基准；控制台极短阶段的速率不用于ETA。后续计时改用perf_counter，原结果不重跑或覆盖。
见[本次摘要](reports/work-ii-information-completeness-20260906.md)。以下保留执行前固定设计；
整个干预尚未完成，不能把96个kernel单位称为96个科学Agent实验。

## 问题与唯一干预

相同实验观测下，公开从候选系数到测量值的完整映射，是否改善未知/错误先验下的结构恢复？
两条件只改变公开合同的完整性：原候选公式说明 / 候选公式加过程、观测和噪声合同。
相同world、数值证据、候选、先验、schema、两轮流程、模型、可选数值工具与资源规则；
两组独立fresh thread，组内pre/post同thread。完整合同只在post交付，不附正确family、
目标指数、私有seed、隐藏评分、参考拟合答案或演算示例。信息长度属于干预，不宣称隔离纯语义。

原B3最终观测发生在分相与取样之后。接触器的平衡分配式不是完整终点映射；不得直接以
接触器校准通过冒充最终四指标、历史公开包或参与者任务已可识别。

## A：本次固定离线可行性块

运行前固定以下覆盖，不使用新provider、完整ActionPlan或历史隐藏评分来拟合：

- 从current的W2-77协议解析原manifest；所有5个公开包检查基础查询合同是否相同，不选择有利world。
- 固定取首包全部8证据+8评分查询的公开坐标，共16个；这里仅用评分查询的输入，不读取评分值。
- 两个声明的校准状态：T=293.15/303.15 K；总product=0.02 mol、impurity=0.003 mol；
  额外初始水相体积0.020 L；系数倍率和相体积倍率均1。它们是人工校准上下文，不是新增独立world。
- 三个核验指数1.0/1.3/1.75，共2×3×16=96个kernel调用；每个只调用一次，失败全部保留。
- 公开参考实现只使用显式输入、指数和所写方程；不导入私有模拟器。校验驱动可以独立调用
  当前production partition kernel作为对照，记录这属于privileged implementation validation。
- 测量：product分配系数、organic/aqueous质量分数的最大绝对偏差；用反解从organic读出估计指数。
  反解只接收读数及公开坐标，不接收目标指数；它以幂律族为条件，不进行family选择。
  kernel偏差≤1e-10、反解指数偏差≤1e-8才通过。
  中间状态域无效或反解奇异计失败，不能删除或替换坐标。
- 96单位结束即停止本阶段；失败表示该参考公式或适用域仍未资格，不据此否定Agent能力。
  即使全部通过，完整终点合同、噪声处理及同信息参考求解仍为未完成，禁止启动正式Agent块。

输出为一个脱敏JSON及机器生成的可读摘要，包含96行、全部失败、时间、复用/新增world数和解释边界；
代码包含纯公开参考函数及独立核验驱动。重复运行不得覆盖本次结果。当前绑定仅新增本开发结果，
不改写历史B3资格判定或回填主分析。

## 阶段A之后的最小矩阵（执行前设计及后续冻结记录）

### 2026-09-06执行顺序及阶段B固定设计

用户已明确授权“先跑完GPT”。执行顺序为参考资格→GPT 6开发→GPT 60正式；
DeepSeek的6开发/60正式后续单独执行，保持同一科学对照，不等待其开发才启动GPT。
GPT开发预算固定每轮600秒、每会话1200秒、全块2小时、provider重试0；正式预算在开发后固定。

本次B块在任何新数据前固定：保留原5个公开包及其全部8证据/8留出坐标，
另生成1个独立开发world（私有seed只写ignored运行目录，与原world及旧开发seed不同）。
开发world在相同8证据坐标上执行线性参考与目标幂律，在8留出坐标执行目标幂律；
共24个完整ActionPlan、24次exact replay。目标开发指数固定1.75，仅供生成器和资格评价，
不进入参考拟合输入。同world同query的参考/目标共用观测噪声坐标，不同query分别绑定；
公开说明这种配对及逐指标Gaussian标准差、[0,1]裁剪和float32读出。

参考输入仅为完整公开合同、公开证据及未标注的留出坐标；先封存所有候选的拟合与预测，
再读取留出评分。完整合同含真实初始温度、初始体积、加液顺序、混合/夹带、分相、
取样及指标归一化。旧包对当前合同的适用性另列，不把历史运行语义默认为当前语义。
候选参数域两信息组相同：power alpha∈[0.25,3]；saturating a∈[0,20]、b∈[0,100]、
c∈[0.01,100]；constant为每个观测指标的[0,1]常数；linear无拟合参数。
饱和族用多起点有界最小二乘；幂律用全域粗网格后局部有界优化，不读取目标答案。
拟合只对独立organic通道计残差，不重复计算其派生score；家族选择须报告全部竞争者，
不以只在power族内反解代替family资格。

通过条件：24/24完整执行与零容差replay；实现映射对无噪声终点误差≤1e-7；
公开配对证据下power为唯一最小残差族（与次优的organic RMSE差≥1e-4），指数误差≤0.10；
8个留出query四指标平均绝对误差≤0.02。报告历史5world的同信息拟合与误差，
历史合同不相容时明确列为不适用，禁止把开发资格回填旧B3。
任一开发资格条件失败，B块以失败终止，GPT正式与新10world暂不启动；
不换seed、坐标、阈值或追加采样。平台缺陷可修复，但受影响块须另行记录并整体重资格。
输出一个机器JSON/可读摘要、24条执行/replay收据及封存的公开参考预测；原始轨迹留在ignored目录。

### B1终态与B2平台配置修复（B2执行前固定）

B1完整24/24执行和24/24重放完成，终点映射0/24通过；原配置缺少scoring_contract_id，
实际使用task-derived-scoring-v1，未启用公开候选所要求的独立solvent/extractant表与v3归一化。
这是运行合同错配，不是Agent作答。B1报告与全部失败永久保留，GPT调用仍0。
B2仅把新块的runtime显式绑定partition-s0-extraction-efficiency-v3；不改production物理实现、
不改旧报告。使用B1同一私有开发seed、同24单位、同目标指数、同测量和通过阈值，整块从头重资格。
输出独立B2报告，current同时保留B1/B2来源。旧5world诊断复用B1分析，不重复计算或升级结论。
若B2仍不通过，保留其失败并继续按上述停止规则处置；不以改阈值取得资格。

### C：十world刺激材料资格（执行前固定）

B2通过：24/24执行、重放和终点映射；公开参考指数误差4.18e-8，四指标留出MAE0.00649。
GPT 6开发会话开始后，十world覆盖固定为十个独立私有生成seed，分别绑定指数
1.35、1.45、1.55、1.65、1.75、1.85、1.95、2.05、2.15、2.25。
这是同一幂律族内的参数覆盖，不是十种结构，也不把query/模型当作新world。
仍用原16个公开坐标，不按新成绩筛选。seed与B1/B2开发和旧B3排除重叠，首次运行前封存，
与模型无关。每world 8线性参考+8目标证据+8目标留出，共240执行和240零容差重放。
每world重复B2的映射、四家族拟合与封存后留出评估，全部采用B2同一阈值。
任何world失败均保留，完成固定分母后停止正式Agent，禁止换world或仅投放通过子集。

此块为固定刺激材料的离线资格，formal_result=false，不作为正式Agent性能结果；
正式Agent冻结时逐字绑定已资格的公开刺激和隐藏评分输入。正式推理覆盖10world，
每model 3先验×2信息组=60会话。主对比只含opaque/misindexed，按world等权；
GPT先单独报告，双模型齐备后再按world内两模型平均，不把GPT阶段结果当作双模型结果。
bootstrap固定seed90870、20,000次world重采样、95%百分位近似区间；只有块终态才报告区间。
正确先验保留、连续指数误差、预测MAE、regret与工具用量另报。

旧数据合同核对：从current解析的原B3五world、16坐标、linear/power两分支共160条既有轨迹，
首条记录的scoring_contract_id全部为task-derived-scoring-v1，来源提交均7caa3dd1748e71ac25aa5873b04e6210dcef0791。
这是只读历史核对，新增物理执行0。当前v3完整公式不能直接用于旧B3；
新干预的两信息组共同使用修复后的v3环境，干预组间仍只改变post披露。
旧F2结果必须继续按“公开可识别性未建立”的边界解释，禁止写成Agent无法发现正确公开机制。

### D终态与GPT正式冻结

C 10/10 world资格通过，240/240执行及重放；最大映射误差4.44e-16、指数误差1.34e-7、
单world四指标留出MAE最大0.00709。连同B1/B2累计288次完整执行和288次重放；其中B1的24个失败保留。
GPT开发6/6有效完成、12/12轮usage完整，6个独立thread，组内续接全部一致；
总耗时805.906秒，单会话98.5–192.9秒，单轮22.5–154.7秒，无provider/schema/platform失败。
数值工具可用但6会话均未采用；本块不要求采用，也不把模型答对作为技术通过条件。
开发恢复为original 0/2、complete 2/2；正确先验保持两组各1/1，仅作开发记录。

正式GPT固定60会话/120轮；不改上述十world、指数、坐标、先验、披露、schema、顺序或主读出。
每轮600秒、每会话1200秒、整个GPT块14400秒（4小时）、provider重试0；
平台/越界工具故障立即停止后续单位，provider或schema失败保留并继续；不重发已尝试会话。
开发实测折算正式8059.06秒＝2.24小时，软ETA 2–3小时，硬预算不等于保证完成时间。
正式输入绑定已资格的刺激材料；以单个干净main提交冻结实际Agent执行面和输入一次后启动。
DeepSeek仍为后续6开发/60正式，其预算未校准，不占本次GPT完成条件。

以下三项保留最初双模型规模方案；A/B/C及GPT的实际终态与正式冻结以上文为准，
DeepSeek仍待开发校准，不按早期预算自动启动。

1. 完整合同与参考校准：在原5公开world加独立开发world上验证分相、取样、指标归一化、
   噪声及评分映射。参考拟合只接收拟交付给Agent的完整公开合同和证据，评分标签在预测封存后评价；
   线性/幂律/饱和/常数的参数域必须明示。若仍不能区分结构，停止进入Agent正式块。
2. 双模型开发：1个与正式分离的world × 3先验 × 2信息条件 × 2模型 × 1次＝12会话/24轮。
   工具两组均可用，技术通过不以Agent科学作答正确为条件；报告长度、工具使用、时限与用量。
3. 正式预算方案：10个新world × 3先验 × 2信息条件 × 2模型 × 1次＝120会话/240轮。
   GPT/DeepSeek各60；未知/错误先验80会话用于恢复主分析，正确先验40用于保持。
   用增加world代替同world重复；10个world的覆盖生成规则、与旧数据隔离和充分性校准须在看正式
   Agent答案前固定，不按旧失败或新结果选world。此处为最初规模方案，不是功效保证；
   GPT的60会话分母已按上文正式冻结并完成，DeepSeek的60会话仍待技术/成本校准。

主估计为完整−原说明的failure-aware joint-recovery差，先在world内平均模型与未知/错误先验，
再对world等权；保持另列，不能混入恢复主效应。沿用family正确及指数误差≤0.10作为恢复定义。
逐world、逐模型、连续指数误差、留出预测误差、决策regret和工具采用率均报告；近似区间按world
重采样，唯一主对比及bootstrap设置在正式前冻结。副本、模型、queries不增加独立world数。

阶段A通过后仍需完整参考校准；其失败不能用增加模型解决。若合同补全有益，结论仅为该披露策略
在此设置的效应；无益也不能证明内部缺陷，需结合可识别性与实际工具采用；两组均成功也是有效结果。
任何结果都不触发补到显著或换world。新块独立保留所有失败，平台修复遵循AGENTS的整块重资格规则。

DeepSeek后续provider预算可用每轮600秒、每会话1200秒、开发4小时、正式18小时作为历史参考，
不能自动视为已冻结；以其新6会话开发重估并在正式首单元前固定。无隐藏重试，完整记录usage缺失。
阶段A未读取凭据；GPT已完成现有直连入口的开发和正式块。DeepSeek仍须单独验证当前入口，
不经过第三模型Responses→Chat桥接器。
