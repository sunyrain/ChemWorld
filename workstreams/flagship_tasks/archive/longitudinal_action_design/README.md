# Archived longitudinal action-design branch

This directory is historical and non-authoritative. Current Work II coordination is owned by
`workstreams/flagship_tasks/WORK_II_TODOLIST.md`; the current comprehensive-decision design is
`WORK_II_AS_OPEN_ACTION_DECISION_EXPERIMENT_NOTE.md` plus
`configs/benchmark/work_ii_as_open_action_decision_v0.1.json`.

Retained here:

- the unexecuted W2-40 full-32-prediction design note;
- the terminal W2-44 fixed-grid matched-action note;
- the terminal W2-45 analytic-crossover note;
- the terminal W2-46 corrected-volume/common-noise note.
- the superseded W2-47 feature-only terminal packet note, config, pilot note, pilot launcher,
  design materializer, and tests.

The W2-44--W2-46 configs are preserved under `configs/` in this archive. The unexecuted W2-40 config
remains at its original benchmark path only because W2-43 uses its candidate-grid fields as a
historical input; it is labelled historical and has no current launcher. Raw truth, exact replay,
and provider accounting remain under ignored run roots; the concise terminal interpretation remains in
`reports/WORK_II_AS_LONGITUDINAL_MATCHED_ACTIONS_CLOSEOUT_ZH.md`.

The obsolete launchers, matched-action implementation, and exclusive tests were retired as one
producer-consumer island. They must not be restored as a competing current entry point. Retained raw
truth reports contain the exact compiled action plans, outcomes, replay checks, and denominators needed
to interpret the terminal negative results.

The W2-47 single-world three-arm provider run is retained as a development protocol diagnostic only:
its raw records remain under the ignored `runs/development/` root, while its feature-only terminal
packet and launcher are archived here. The diagnostic exposed that the public candidate feature vector
did not disclose the full executable operation plan; it is not a clean action-quality result.
Its concise closeout is `reports/WORK_II_W2_47_FEATURE_ONLY_PACKET_DIAGNOSTIC_ZH.md`.
