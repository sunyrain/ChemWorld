# W2-132: English EC/PA five-world budget matrix

Fixed before execution, 2026-09-19. Initially one executor on main; scheduling amendments below record subsequent user-authorized concurrency. GPT-5.6 Sol / medium. The user authorized both EC goals, both budgets and all five worlds, with E completed before single-world P/S pilots. This is a new development evidence block, not retrospective qualification of the old sources.

## Question and coverage

How do entity priors and a 12-versus-24 experiment budget affect autonomous research, mechanism reports and predictions? EC has separate discovery and public-score optimization assignments; PA has one partition-discovery assignment. New participant instructions, K1/Q/K2 questions and generated result reports are English. Historical Chinese outputs remain unchanged.

E coverage is EC: 2 goals × 2 budgets × 5 worlds × 3 arms = 60 sources; PA: 1 goal × 2 budgets × 5 worlds × 3 arms = 30 sources. Total 90 sources, 1,620 planned source batches, at most 1,620 additional measurements plus 1,620 final assays, and 270 posttest submissions. Each cell is attempted once. The 24-batch source is a new session, not a continuation that has seen prediction questions. Within-source grouping, repeated conditions, optional calculator fitting and free mechanism forms remain allowed; no forced per-batch reflection or hypothesis template.

Worlds are the existing EC-W01–W05 and PA-W01–W05. EC uses seeds 0–4 with the nominal-prior-latent-v2 physical family and each world's registered E transposition. PA uses seeds 0–4 AND its registered distribution-coefficient/phase-volume interventions. These world definitions come from the EC entity repair and PA qualification configuration; their historical release gates do not authorize or certify this separate development block. World IDs and prior-arm labels stay host-side.

Execution order is world → budget 12 then 24 → EC discovery, EC optimization, PA discovery → three arms. The arm order rotates with world index. Order is fixed before outcomes. Separate sources do not share evidence. All arms and budgets in a world share identical physics and prediction questions. E priors are anonymous catalog only, nominal dossier, or transposed dossier. PA's transposition is X0/X3; EC uses its world-specific mapping. Nominal marginal material information is not a complete law or a guarantee for every pairing.

## Reference, observations and outcomes

Before sources, check 60 actual published agent contracts/material replies: 2 systems × 5 worlds × 2 budgets × 3 arms. Check the advertised budget, English input, anonymous identifiers, absence of arm/truth identifiers and correct dossier availability. These resets use no provider and perform no physical actions.

Generate one fixed prediction reference per system-world: 10 sets × 12 conditions = 120 batches, 900 operations (EC 60×6; PA 60×9), with another 120 batches/900 operations for exact replay. Every reference must terminate, yield finite required measurements, avoid rollback and replay exactly; PA pre-removal HPLC truth must close. Responses and all failures are retained; neither high yields nor a preferred model ranking are pass criteria. Do not change questions, worlds, stopping rules or sample size after seeing outcomes.

EC uses W2-131's 12 polarity/context questions unchanged, with six final metrics and 80% prediction intervals; report aggregate and domain-wise errors/coverage/width. PA retains its 12 recipes, two pre-sample noiseless target fractions, 90% intervals and two decisions. K1 → Q → K2 uses the original source thread, without new laboratory access or truth feedback. K2 has three questions. No LLM judge is a primary endpoint in this block. Preserve non-English model answers if instructions are violated, flag them, and do not repair them by re-asking.

An EC source may nominate a completed recipe. Its fixed independent recommendation retest is at most one extra batch per source (up to 60 E retests), using the same world and a different observation seed, followed by exact replay. Missing recommendations are retained, not replaced. Source replay and retests are separate resource counts, never additional independent sources. All source actions, intermediate observations, final assays, rollbacks, K1/Q/K2, incremental token accounting and failures remain readable.

Each source has 30×budget operation capacity, budget-matched measurement/stock capacity, and no hidden process-time cap. Source wall allowance is 150 seconds per planned batch; token limits and history capacity scale with budget. Each posttest retains the existing 1,200-second ceiling. These are maximum resource envelopes, not target run times. No provider retries, source replacements or success-conditioned exclusion. An intact terminal context can be interviewed even when source completion fails.

## Failure and stop rules

Scientific errors, poor predictions, wide intervals, failure to identify a mechanism and lawful inefficient choices remain results; continue the planned denominator. Keep partial sources and all unstarted units. Stop the queue on failed reference validation, a detected input leak/contract defect, source startup failure before any physical action, or failed exact replay. Do not automatically restart a partial source. Platform repair retains the affected original block; an affected formal block would require a new execution version, not an overwrite. A report-rendering failure may be repaired from retained data without model reruns.

## Network recovery amendment (2026-09-19)

The user explicitly authorized retrying network failures after the first three E sources. This supersedes only the original no-network-retry rule; the saved original design and failed attempts remain unchanged. EC-W01/B12/discovery/Aligned completed all 12 batches and exact replay, then lost K1 transport. Resume its saved original thread for K1, Q and K2, with the identical questions and no laboratory access or truth feedback. Preserve the failed turn in that thread and record that its failed reasoning may remain in context. EC-W01/B12/discovery/MisIndexed failed before any physical action; retry it in a new directory and session with the identical world, prior, goal, model and budget. Opaque is complete and is not repeated.

For this matrix, permit at most one recovery attempt per source when raw provider error events positively identify a network/transport failure. A completed, exactly replayed source may resume only missing/failed posttests; a zero-action source may start fresh. Partial physical campaigns, authentication/quota failures, changed threads, invalid submissions and unfavorable scientific outcomes are not automatically retried. Stop on a repeated network failure or an unresolved physical/replay failure. Continue the unchanged source order after recovery. Keep first-attempt and recovered results separately; 90 unique E sources remain the denominator. Report additional source attempts, physical batches, posttest attempts, elapsed time and available token usage separately. Missing usage is unknown, not zero. Same-thread cumulative usage is differenced, never added twice. References, prior successful stages, and recommendation retests are not repeated during posttest recovery.

## Host-reboot recovery amendment (2026-09-19, before continuation)

The user requested continuing this matrix after the executor disappeared. Windows reports a reboot at 03:00:43 +08:00. Nine sources had completed; EC-W01/B24/discovery/E/Opaque was interrupted after 137 logged operations, 19 final assays and one open batch, without a terminal provider receipt. Its provider rollout and laboratory files survive in the abandoned temporary directory, but the in-flight tool process and simulator process do not. This is infrastructure loss, not a scientific outcome or a network retry.

After verifying the retained trajectory by exact replay and matching its provider thread to the surviving session, close and retain the interrupted attempt. Run exactly one fresh 24-batch attempt for this named source with the saved questions, world, prior, model, goal and resource envelopes unchanged. A fresh attempt is explicitly labelled as a replacement attempt; it is not described as continuing the interrupted 19-batch campaign. Do not repeat the nine complete sources or prediction references. Charge the retained 19 batches, all 137 operations, verification replay and all available partial token usage separately, and distinguish physical totals across attempts from the current logical-source budget denominator. Missing interrupted usage remains unknown. Resume the unchanged queue afterward. This incident-specific amendment does not enable automatic replacement of arbitrary partial sources or unfavorable outcomes.

## Two-source concurrency amendment (2026-09-19)

The user requested moderate parallel execution. From this handover onward, use at most two independent source processes, with one coordinator as the sole matrix-summary writer. Source instructions, model/effort, worlds, priors, experiment and measurement budgets, questions, references and evaluation remain unchanged. Each source retains its own agent thread, laboratory, simulator, output directory and resource ledger; neither source receives another source's evidence. This changes execution scheduling only, not within-source autonomy or the planned 90 E plus 12 EC P/S denominators.

Let the currently running serial source finish. Reserve the next unstarted source directory for the new scheduler, using that existing ownership boundary to prevent the old serial worker from starting it. The new worker may execute this reserved source while the old worker finishes its current source; the new coordinator takes over the shared report only after the old executor exits. Record this deliberate handover separately from scientific and infrastructure failures. Continue dispatching in the existing unit-list order, although completion order can differ. Begin P/S only after all E sources are terminal. On an unrecovered infrastructure defect, stop dispatching new work, retain the other already-running source to completion and preserve every failure. Retain the existing bounded network-recovery policy. Report active workers, their batch/posttest stages, actual wall-clock throughput and ETA; do not sum overlapping source durations as elapsed wall time.

## Deadline concurrency and partial-network recovery amendment (2026-09-19, before resume)

The user requested further parallel execution to finish before 19:00 Asia/Shanghai. Increase the maximum to four isolated source processes, retaining one coordinator and the existing E-before-P/S order. This supersedes the two-source scheduling limit above. Do not change the source model, prompts, scientific questions, reference answers, budgets, outcome thresholds, planned cells or within-source autonomy. Runtime resource checks show approximately 40 GiB free RAM; API throughput under four sources remains an empirical estimate, not a guaranteed deadline.

The previous coordinator has stopped after both workers exited. It retained 31 complete sources and one EC-W02/B24/optimization/E/Aligned source interrupted after 8 final assays and 56 operations. Provider error events explicitly report a disconnected response stream / network decoding error; exact replay of all 56 operations passed. Its simulator process has exited and the current source runner has no supported append-only simulator/session restoration path. Under the user's standing network-retry authorization and request to resume faster, retain the partial attempt and make one separately recorded fresh attempt of this logical source with its saved design. Do not splice its eight batches into the replacement or overwrite the first result.

Apply the same bounded rule prospectively during this resumed block: only explicit network-error events, a failed incomplete physical source and a verified original replay permit one fresh replacement; quota/authentication/rate-limit failures, invalid replay, scientific outcomes and already-attempted recoveries do not. Original and replacement physical work and available tokens are both charged. Complete sources still recover only missing posttests in their original thread, and no successful source or shared reference repeats. A failed recovery stops new dispatch while siblings finish. All scientific failures remain results with the original denominator. This is an infrastructure-recovery amendment, not a change to scientific stopping or scoring rules.

Report complete, failed and terminal source counts separately. While draining after an infrastructure failure, show the stop reason immediately and suppress ETA. Use wall-clock throughput for the current concurrency segment; the 19:00 target is a reporting deadline, not an automatic source truncation or a reason to skip the fixed queue.

## Network-restored continuation amendment (2026-09-19, before second recovery)

At 13:52 Asia/Shanghai the user confirmed that the network had recovered and requested continuation. The stopped coordinator retains 42 complete sources, four failed sources and 56 unstarted sources. Authorize exactly one additional recovery for the following four sources, stored as `attempt-2`; preserve their originals and `attempt-1` unchanged. This supersedes the one-recovery ceiling only for these named sources and this incident. New sources retain the existing bounded recovery rule.

- PA-W03/B12/discovery/E/MisIndexed: the 12-batch physical source and K1 are intact. Resume the latest saved original-thread context for Q and K2 only.
- PA-W03/B12/discovery/E/Opaque: the 12-batch physical source is intact. Resume the latest saved original-thread context for K1, Q and K2.
- PA-W03/B12/discovery/E/Aligned: retain the original two-batch campaign and the zero-operation first recovery; execute one fresh 12-batch source.
- EC-W03/B24/discovery/E/MisIndexed: retain the original 13-batch campaign and the zero-operation first recovery; execute one fresh 24-batch source.

Eligibility is checked against each latest failed attempt's provider error events, not a stale earlier network error. Posttest recovery uses the latest saved rollout while keeping the original source receipts, physical trajectory and recommendation retest. Previously completed posttests are not asked again. All recovery attempts remain separately linked and charged; posttest token increments are differenced against the immediately preceding result. Four isolated workers and one summary writer resume the unchanged remaining queue, with E before P/S. Repeated infrastructure failure stops new dispatch while siblings finish. No scientific inputs, thresholds, questions, outcomes or denominators change.

## Further intermittent-network continuation (2026-09-19, before next attempts)

After the previous resume, 46 campaigns completed and the coordinator drained to a stop. The user again requested continuation during the analysis/writing turn. PA-W03/B12/discovery/E/Aligned attempt-2 retains eight final assays and 88 operations with verified replay and explicit transport errors; authorize one fresh 12-batch attempt-3, retaining all earlier work and costs. EC-W03/B24/discovery/E/Aligned attempt-1 completed all 24 batches, replay, K1 and Q, then encountered a provider usage-limit rejection at K2. The prelaunch diagnosis caught this distinction and no additional model attempt was started while quota was unavailable. The user subsequently explicitly confirmed that quota had been restored; authorize attempt-2 for K2 only, using that replacement source's saved thread and trajectory. Do not repeat its physical campaign, K1, Q or recommendation retest.

Resolve the next recovery number separately for each failed source at this explicit resume. Quota restoration is an explicit incident-specific option for intact posttests, not a transport-error classification or a prospective automatic quota retry. This is a single additional attempt per named source, not an unbounded automatic retry policy; new source failures still have at most one automatic network recovery and a failed recovery stops dispatch. Keep four independent workers, the original queue, all scientific settings and all failed attempts. Completion numbers and the manuscript's complete two-world analysis remain separate from runtime availability.

## Fifth-world network continuation (2026-09-19, before 16:50 resume)

The coordinator stopped at 15:51 with 75 complete campaigns. The 16:47 status inspection found no live workers; both latest failures are explicit response-body transport errors, not quota, authentication, scientific outcomes or replay defects. Under the user's standing instructions to continue the experiment and retry network failures, make one additional, separately recorded recovery for each affected source. EC-W05/B12/discovery/E/MisIndexed retains its 12-batch source and verified 84-operation replay: attempt-2 resumes the latest saved original thread for K1/Q/K2 only. EC-W05/B12/optimization/E/Aligned retains the original two final assays and a first recovery interrupted after six operations with zero final assays and verified replay: attempt-2 is a fresh 12-batch source. Keep all prior attempts and costs, every completed campaign and all references unchanged. Resume the existing four-worker queue with E before P/S; this adds one bounded recovery per named source, not an unlimited retry loop or a change to the scientific design.

## Abrupt process-tree interruption (2026-09-19, before recovery)

The user explicitly requested continuation and diagnosis after the 17:17 interruption. The last coordinator heartbeat is 17:17:11; source operations stopped around 17:17:14. Coordinator and all four workers are absent, worker stderr logs are empty, no normal terminal cleanup occurred, and Windows has not rebooted since 03:00:43. The exact initiating cause is unconfirmed; do not relabel this as a diagnosed reboot, quota rejection or transport failure. Preserve the stale running ledger as incident context and reconcile it from retained outputs before resuming.

Retain all 83 completed campaigns and shared references. EC-W05/B24/discovery/E/Opaque's original 22-batch network failure is already retained; its interrupted first recovery completed 24 batches and replay but lost K1. Seal that recovery separately and use attempt-2 for K1/Q/K2 only. EC-W05/B24/optimization/E/Aligned completed 24 batches, replay and K1; use attempt-1 for Q/K2 only. Restore both latest original threads from matching surviving temporary homes. Their sealed recommendations have not yet been retested: execute each already-planned retest once, verify replay, and charge it separately. No retest output is shown to the agent.

For EC-W05/B24/optimization/E/MisIndexed and Opaque, retain every durable operation and open batch, verify the entire saved prefix by exact replay, preserve the matching provider rollout and available usage, then execute one separately recorded fresh 24-batch attempt each. No terminal provider receipt is invented, and partial batches are not spliced into replacement sessions. The two intact sources' physical campaigns and completed answers are not repeated. All source/recovery attempts, physical costs and verification costs remain visible. Resume four isolated workers and the unchanged queue using an independent background coordinator; recovery remains bounded to one new attempt per named source under this instruction. Scientific settings, questions, denominators and E-before-P/S order remain unchanged.

Recovery verification: the two partial prefixes contain 100 operations/13 final assays and 119 operations/17 final assays; both replay exactly, adding 219 verification operations. The two pending recommendation retests used eight and seven operations respectively, with exact replay. All 51 focused recovery/coordinator checks passed. At 17:31 a detached Windows coordinator was started with a new process group and breakaway from the launching job; startup returned successfully and subsequent independent checks observed live workers, fresh heartbeats and new source operations. Wrapper stdout/stderr remain under the user temporary directory. This mitigates lifetime coupling to the launching tool; it does not establish the initiating cause of the earlier termination or promise immunity to arbitrary external process termination.

## P/S follow-up boundary (unchanged)

### Evening status reconciliation and continuation (2026-09-19)

The user explicitly requested restarting all stopped experiments. The coordinator ledger stopped updating at 18:17, but two isolated workers subsequently sealed valid results: EC-W01/B12/discovery/S/MisIndexed at 18:20 and EC-W01/B12/optimization/S/Opaque at 18:22. Reconcile these two completed recovery records into the existing ledger without new provider calls or repeating their physical campaigns. Preserve the previous ledger and all original failures. This leaves 100/102 completed campaigns, not 98/102; the exact cause of the coordinator's disappearance remains unknown.

After verifying that no owner processes remain, authorize one additional recovery (attempt-2) for EC-W01/B12/optimization/S/Aligned. Its original one-operation attempt and zero-operation first recovery both have explicit provider transport errors; retain both and their costs. Start the still-unattempted EC-W01/B12/optimization/S/MisIndexed once. Use the existing isolated-worker coordinator, unchanged saved scientific design, 12-batch budget, priors, English prompts, questions and evaluation. Completed sources, references and retests are not repeated; quota/authentication/scientific failures are not reclassified as transport failures. The remaining two sources run concurrently within the existing four-worker limit, with bounded recovery and regular progress output.

Evening outcome: both sources completed all 12 batches; MisIndexed also completed K1/Q/K2. Aligned attempt-2 completed K1 and its recommendation retest, then Q was terminated by the inherited diagnostic calculator limit. Its Q log contains eight successful calculator calls followed by four rejected calls, with no provider transport error. The shared diagnostic launcher sets `--limit 8` and kills the turn after more than eight audited attempts; this is an execution-budget failure, not evidence of incorrect scientific predictions. No Q payload was sealed and K2 was not attempted. The frozen block therefore retains 102/102 attempted sources, 1,764/1,764 effective physical batches, 101 complete chains and 304/306 posttests. All processes have exited. Do not retry this as a network incident or overwrite it with a more generous-budget result. A future explicitly versioned posttest design should make calculator limits appropriate to the question set and disclose them; historical eight-call consumers remain reproducible.

After all 90 E sources reach terminal states, the currently executable EC priors support a preselected W01/12-batch pilot: P and S × 2 goals × 3 arms = 12 additional sources, 144 planned source batches and 36 posttests, with the same EC-W01 reference. No five-world P/S expansion. PA P/S are not silently implemented by reusing E or naming a hidden family: their current public-prior definitions and applicability require a separate design/check before any PA P/S source. This limitation will be reported explicitly; it does not block E. The optional EC P/S queue is frozen by the command-line flag and cannot begin before E is complete.

## Subsequent calculator repair (new designs only)

After this block reached its terminal state, the user requested correction of the eight-call posttest failure. New EC v5 / PA v4 designs and newly created matrices save an explicit budget of 128 public calculator attempts per K1, Q and K2 stage. Scalar and array calls share the allowance; invalid expressions count. Tool replies disclose usage and remaining attempts. Exhaustion rejects further calculations but permits answer submission within the existing response deadline. The calculator and launcher receive the same saved policy. The old tool description already disclosed eight calls; the defect was reusing that small limit and its whole-turn termination behavior for the expanded follow-up task.

Existing saved designs without the new policy retain the historical eight-call hard stop, including fresh PA source recovery under an old matrix. No historical question, source, sealed answer or failure was overwritten, and the failed EC posttest was not rerun as part of this repair. Actual local calculator/launcher fixtures and related checks passed 93 tests with no provider calls (six unrelated CLI credential/retry cases were deselected). This is execution validation, not a new completed scientific result. The terminal denominator remains 101 complete chains and 304/306 posttests.

## Outputs and ETA

Raw output: `runs/development/ec-pa-five-world-en-20260919-v1/`; readable English report and source outputs: `reports/work-ii-ec-pa-five-world-en-20260919/`. One saved design and one progress/result ledger; no additional global audit package. Raw provider data, credentials and ignored runs do not enter Git.

E estimate: 15–23 hours at the historical 10–15 minutes/source, one executor. EC P/S adds approximately 2–3 hours. Update throughput/ETA from actual completed sources and emit a heartbeat at least every 30 seconds. The full authorized E denominator is 90, regardless of failures; P/S is reported separately. The new questions/language/budget/world support are fixed before launching this block.

## Preparation incident: replay configuration (2026-09-19)

All 60 public-entry checks passed. EC references for five worlds and PA-W01 passed. PA-W02 executed all 12 batches/108 operations without rollback, but its verifier had omitted the explicit world-intervention manifest and therefore replayed the default world. The original failed `reference-result.json` is retained. This is a post-execution interpretation defect (class C): source inputs, actual physics, query selection and measurements were correct, and no model sources had started. Correct only the replay call, validate the retained trajectory with its saved intervention manifest, and bind a separate corrected replay result; do not repeat physical references or call a model. Charge the additional 108 replay operations separately. Continue untouched remaining worlds only after the corrected verification passes. This applies the existing interpretation-repair rule without changing scientific questions, thresholds, coverage or stopping rules.
