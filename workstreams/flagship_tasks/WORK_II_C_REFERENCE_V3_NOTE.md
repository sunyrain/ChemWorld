# C reference v3 and formal launch — 2026-09-20

## Post hoc baseline repair and analysis, 2026-09-21

The user authorizes repairing the two public-baseline parser failures and analyzing retained results. This is deterministic reanalysis, not a new participant or qualification block. The recipe extractor now recognizes `campaign_resource_rejected` as a rejected attempt: preserve it as provenance and exclude it from executed recipe features. Unknown statuses still fail. This explicitly changes the postprocessing helper in `scripts/run_work_ii_c_pilot.py`; the historical release binding and original source/result files are preserved, not regenerated or claimed to bind the revised helper. No physics, queries, agent budgets, model outputs or prediction scoring changes.

Coverage is all thirty C sources, including both reported failures. Recompute the existing public-mean and nearest-neighbor baselines using only retained public final assays for fitting and the original saved reference targets for scoring. Require thirty available baselines, two repaired omissions, exact equality with all twenty-eight previously available results, and byte preservation of source trajectories, original results, design and reference inputs. Keep every original failure in the repair summary. Output `BASELINE_REANALYSIS.json` and its readable report beside the current C report; let the current inventory consume that correction explicitly. No simulator or provider calls. Comparisons are exploratory, report all four metrics, both budgets and all arms, and keep the eleven-assay source visible. Test the actual rejected-action feature path and rejection provenance before recomputation.


User authorization: accelerate preparation and begin real model experiments after valid qualification. This is a new block; preserve all old pilot, interrupted formal and development records. Retain full-process v5, the latent material family, world distribution, thresholds, E O/A/M intervention and GPT-5.6 Sol / medium. No physics tuning or new parameter grid.

Question: does a fixed six-pair prediction set around the already demonstrated accessible domain retain material, seed, cooling, thermal-history, continuing-growth and upstream-loading effects, with both quality classes in every planned world? The host reference domain is not a prescribed source-agent workflow.

Fix twelve unique recipes before execution, using the same upstream reaction (catalyst 1, 385 K/1,500 s then 900 s wait), quench, charged 300 K/300 s conditioning, filtration and final assay. Default S0, reagent 0.004 mol, solvent 0.020 L. The pairs are:

1. S1 versus S3, 0.045 g seed, sixteen 14,400 s linear cooling stages to 278.15 K.
2. Seed 0.001 versus 0.045 g, otherwise the default sixteen-stage path.
3. Seed 0.040 g: eighteen-stage linear cooling versus a 120 s quench-cool followed by isothermal holds; same final temperature and total duration 259,200 s.
4. Seed 0.035 g: twenty-stage linear cooling to 278.15 K, then either a 7,200 s hold or 315 K/3,600 s reheating and 278.15 K/3,600 s recooling. Both complete schedules have equal duration and final temperature.
5. Seed 0.025 g: cool to 292 K over 60 s then filter, versus add a 7,200 s growth hold before filtering. Extra time is part of the intervention.
6. Reagent 0.004 versus 0.008 mol; seed 0.045 g, nineteen-stage linear cooling to 278.15 K, all other settings fixed.

Run exactly 24 development reference batches: all twelve in seeds 41 and 47, with exact replay. These worlds were previously inspected and are development checks, not independent confirmation. No candidate selection or threshold changes within this block. Each world must have >=3 quality-positive and >=3 quality-negative outcomes, >=1 quality-feasible recovery >=0.10, >=4/6 resolved pairs, legal execution and exact replay. Quality is purity>=0.80 and fines<=0.50 with particles present. Existing metric resolutions remain unchanged. Maximum 60 operations per reference. Retain every failure; failure holds formal launch.

If development passes, complete focused functional acceptance, commit the stable execution surface once, and create a new frozen design/root. Formal qualification then starts from its first unit: five worlds (seeds 0-4), twelve references and three matched O/A/M instrument probes each, 75 batches plus exact replay. Use the same coverage requirements and check public anonymity, denominator wording, 12/24 budget delivery, identical physics/observations across arms and distinct worlds. Prior development exposure is disclosed; this qualifies a controlled environment, not a new confirmatory scientific finding. Failed qualification is retained and blocks the provider queue without automatic redesign.

After a passed frozen qualification, launch the previously authorized sequential 30-cell block: five worlds x 12/24 source budgets x E O/A/M, 540 source batches, 90 K1/Q/K2 stages and 30 planned recommendation retests. Source behavior remains autonomous. All prompts/reports are English; K1/Q/K2 each has 128 calculator attempts and a 1,200 s deadline. Do not supply the host feasible recipe, hidden diagnostics or prediction truth. Keep the source model, resource card, stage order, scoring, observation noise and no-automatic-retry policy from the existing C formal runner. No P/S or extra goal is added.

Outputs: ignored development/formal roots, saved complete action lists and prompts, machine/readable summaries with all denominators/failures, and live source/posttest progress. Show progress at least every 30 seconds. Development report is separate from formal participant results; old completed evidence is never overwritten or promoted. Do not regenerate global historical audits or bind prose/tests to the runtime surface.

Development acceptance completed before freeze: 24/24 batches and exact replays, 634 execution operations plus 634 replay steps, 113.765 s wall time. Seeds 41/47 have 7/5 and 5/7 quality-positive/negative outcomes, 7/5 feasible witnesses and 5/6 resolved factor pairs, respectively. No recipe or threshold was changed after execution began. All 102 focused functional/scientific checks passed; Ruff passed. Proceed to the new saved 30-source design and full 75-batch qualification from its first unit. Report: [development acceptance](reports/work-ii-c-reference-20260920-v3/REPORT.md).

Qualification completed: all five worlds passed, 75/75 batches and exact replay, 256.109 s. Quality-positive/negative counts are 6/6, 6/6, 6/6, 6/6 and 5/7; resolved contrasts are 5/6, 5/6, 6/6, 5/6 and 6/6. All public-contract and arm-equivalence checks passed.

Operator transport recovery, declared before retry: the first C-W01-B12-E-Opaque session ended after 11.406 s with a provider request connection failure, zero operations, zero batches and no posttest payload. Direct HTTPS timed out; the existing local proxy returned a service response. Preserve the entire original root and failed attempt unchanged. Make exactly one fresh attempt of this zero-action cell in a separate recovery root, then execute the 29 never-started cells in the frozen order. Apply proxy variables only to this experiment process and its children, with localhost excluded. Do not change prompts, budgets, model, task, physics, evaluator, provider error limits or scientific stop rules. Reuse the passed qualification through byte-identical result copies with their original provenance; do not repeat physical references. The saved design and release binding remain byte-identical and must still verify. This is a documented transport exception, not a general automatic retry policy. Any further transport failure is retained for diagnosis. Count the original failed model session separately from the 30 intended effective sources.

## User-authorized completion recovery, 2026-09-20

The user now explicitly requests completing C before proceeding to P. Recovery 1 ended after four complete chains and three source transport failures; no controller remains alive. All three source streams report a disconnected request, not quota, task-quality or agent-budget failure. C-W01/B24/Aligned retained 265 operations and eight completed batches; C-W01/B24/MisIndexed retained five operations and zero batches; C-W02/B24/Aligned made no operation. Both nonempty prefixes already pass exact replay. No failed source saw K1/Q/K2 or hidden prediction truth. A fresh connectivity check returns HTTP 405 through the existing local proxy while direct transport times out; this proves endpoint reachability only, not model streaming reliability.

Before new calls: create a separate completion root with byte-identical frozen design, release binding, passed qualification results and the four completed source directories. Preserve all previous roots. Make one fresh full-source attempt for each of the three diagnosed transport failures, then execute the 23 never-started cells in frozen order. The current runner has no supported append-only restoration of a terminated source's model/tool session; do not splice eight old batches into a new independent source. Record the eight discarded-from-effective-denominator batches, 270 operations and three provider sources separately, in addition to the earlier zero-action provider failure. These are infrastructure recovery attempts, not extra independent scientific samples or retries selected by scientific outcome.

Use the same GPT-5.6 Sol medium model, English prompts, 12/24 budgets, measurements, source/posttest limits, physics, noise, evaluator and recommendation retest. Run one executor. Keep process-local proxy configuration and localhost bypass; do not change global settings or increase the frozen per-session provider-error allowance. Pause the queue immediately on a new provider transport failure for diagnosis, preserving its result rather than spending the next cell during an outage. Other genuine task outcomes remain outcomes. Do not rerun completed sources, passed physical qualification or completed follow-ups. The effective matrix remains 30 sources / 540 source batches / 90 follow-ups / 30 recommendation retests.

Progress and English reports must expose current cell/stage, live batches/operations, completed chains, exact denominators and a 30-second heartbeat. This operational recovery changes transport and orchestration only; frozen runtime verification remains required. No P model calls are launched while completing C.

### Completion recovery 3, declared before new calls

Recovery 2 stopped at 18:44 local time on 2026-09-20 after a provider stream disconnected during C-W02/B24/Opaque. It retained 18 completed batches and 313 operations, all verified by exact replay, with no recommendation or posttest exposure. The controller is no longer alive. Eight complete chains (156 source batches, 24 posttests) remain intact: all six W01 cells and W02/B24/Aligned and MisIndexed. This is a transport failure, not a scientific outcome.

Under the existing instruction to finish C, preserve those eight complete source directories and the frozen design, release binding and passed qualification byte-for-byte in a separate recovery-3 root. Make exactly one fresh full-source attempt for C-W02/B24/Opaque, then run the 21 never-started cells in frozen order. The 18-batch prefix is retained as interrupted consumption, not spliced into a new model session. Aggregate additional interrupted consumption is now 26 source batches, 583 operations and five provider sources across all earlier attempts. Keep every original root. The effective matrix, source model, prompts, budgets, evaluator, transport limits and single-executor policy remain unchanged; pause again on a new transport failure. No qualification rerun or P model call is authorized by this amendment.

Recovery-3 outcome: launched at 19:07, received model responses and completed seven operations, then paused on a new disconnected provider request at 19:08 after 75 seconds. Zero new batches or posttests completed; the seven-step prefix passes exact replay. All eight complete chains remain unchanged. Including this attempt, extra interrupted consumption totals 26 batches, 590 operations and six provider sources. Controller exited; no additional retry is launched while the same transport failure persists. An HTTP response from the proxy did not establish reliable model streaming. Completion ETA is suspended.

## User-authorized automatic continuation, 2026-09-20

The user explicitly requests automatic continuation. This prospectively supersedes the operator-pause-on-every-transport-error policy, before any new calls. The scientific question, 30-source schedule, model, English prompts, physics, measurement and prediction budgets, outcome thresholds and evaluator remain frozen. Preserve the eight complete chains and all historical attempts; reuse the passed qualification. No P model work.

Run one detached executor until all scheduled cells reach a terminal result, with an automatic retry loop restricted to provider error events identifying network/request/response-body failures. Authentication, quota, malformed answers, scientific infeasibility, valid resource exhaustion and replay failures do not trigger this retry loop. Back off 60, 120, 240, 480, then at most 900 seconds between consecutive transport failures, continuing automatically without a retry-count cutoff. Keep a 30-second heartbeat, current cell/stage, next retry time, throughput and completion counts in one stable report. No change to the frozen provider's internal retry/error allowance.

Source boundary: once a failed source is sealed and its nonempty trajectory verifies exact replay, archive its entire directory under `failed-sources/<cell>/attempt-N` in the new continuation root, with its original location recorded. Update the extra-attempt ledger before starting a fresh independent source for the same cell. Do not splice an interrupted prefix into a new model context. Initial effective counts are 8 complete chains / 156 batches / 24 posttests; initial additional consumption is 26 batches / 590 operations / six failed provider sources. Existing successful chains and valid unfavorable scientific results are never retried.

Posttest boundary: retry only a provider transport failure without an accepted structured answer, using the same retained thread and the same question. Preserve partial dialogue and archive each failed stage's raw output/receipt before retry; do not rerun the source or earlier successful questions. Keep the same cumulative calculator audit, so retries do not reset the 128-attempt stage allowance. Record repeated question delivery, failed-stage receipts, elapsed time and available token usage explicitly. The original 1,200-second deadline applies to each execution attempt; extra failed-attempt cost is disclosed. No hidden truth or new laboratory access is introduced.

Validation before launch: transport versus scientific/authentication error classification; complete archival and exact extra-consumption accounting; automatic source retry and progression to the next cell; posttest thread/previous-stage preservation and cumulative calculator limits; frozen source verification and locked-environment lint. The first real continuation is the production-path acceptance. Unknown corruption or changed execution bindings still stop for repair; routine network interruptions no longer require manual relaunch. This is a transport recovery policy amendment, not a new scientific sample or a result-dependent redesign.

### Posttest recovery correction, 2026-09-20 evening

Final provider-free closure, 2026-09-21: the automatic queue ended with all 90 questions
sealed. W05/B24/MisIndexed has a replay-valid 24-batch source and valid K1/Q/K2, but its
public-baseline parser raised on `campaign_resource_rejected` before the planned retest.
Preserve the original result, label that baseline unavailable, and execute only the missing
recommendation retest with the already-selected batch's committed recipe, observation seed
303 and original 24-batch stock envelope. Retain rejected attempts as provenance and omit
them from the executable recipe. No new provider call or source experiment. The W02/B12/Opaque
11/12 shortfall remains a nonconforming scientific source; no replacement is authorized.

### User-authorized four-worker continuation, 2026-09-20

The user explicitly requests four parallel workers. This supersedes the single-executor scheduling policy only. Before new provider calls, retain the frozen 30-cell design, physics, prompts, model, budgets, measurement and outcome rules, qualification and all completed/failed artifacts. Four isolated worker processes may execute independent cells, with at most four simultaneous source/posttest provider sessions in total. Each source remains sequentially adaptive and each K1/Q/K2 chain stays in order. Repairs have priority in frozen schedule order; remaining unstarted sources follow that order as worker slots become available. Scheduling never selects based on scientific scores.

First parallel acceptance exposed revoked refresh tokens in the four old isolated posttest homes. Fresh source sessions use the current local login and have responded. Retain these new authentication failures. For the existing posttest-only repairs, permit copying the current authorized local login into the old isolated home only when it differs from that home's revoked credential; never print or publish credentials or alter the retained thread. Keep every repair result in a new numbered repair directory, including calculator-audit continuity. A dormant final repair sweep waits for the four-worker source queue to end normally, then uses up to four slots for those existing posttests; it does not add a fifth simultaneous model session or rerun sources. Authentication is not relabelled as network failure and there is no automatic loop retrying the same revoked credential. The complete matrix, model and evidence rules stay unchanged.

Stop the dormant repair waiter after verifying its process identity and absence of provider children. Transfer the old source controller only at a transport-backoff boundary with no provider children and no running source result, or after normal completion. Preserve its final state. Do not terminate an active scientific session to increase concurrency. The replacement coordinator alone writes the shared report and failed-source ledger; workers own disjoint source directories and communicate through per-worker status/results. A crashed worker is retained for diagnosis, never replaced by a fabricated success. No completed source is rerun. Source transport failures are archived and requeued with the same capped exponential backoff; repaired posttests use the corrected fresh-directory handling and SSE classifier, retain their original threads and cumulative calculator audits. Unknown integrity failures and source shortfalls remain explicit outcomes. Continue automatic recovery without P model calls. Qualification is reused unchanged. The stable automatic report now exposes four worker slots, completed/remaining jobs and live stages, with a 30-second heartbeat; retry waiting is shown separately from model execution. Validate independent-job selection, concurrency cap, single-owner accounting and the real existing posttest launcher before launch.

The first real automatic posttest retry exposed a wrapper defect: the failed Q directory was copied to an archive but left in place, whereas the frozen launcher requires a new output directory. W02/12/MisIndexed retains all 12 source batches and K1; no second Q provider call occurred after that directory error. W02/12/Opaque retains 11 source batches and K1, but its Q failed with an SSE idle timeout that the network classifier omitted. Its source batch shortfall is a retained outcome, not a reason to rerun the source.

Correct the orchestration only: move each sealed failed posttest directory to its archive after verifying both paths are inside the source folder, keep the calculator audit in place, and classify explicit provider SSE idle-timeout events as transport failures. Test through the real strict-directory subprocess launcher, without model calls, in addition to the state/accounting tests. The running source controller has already loaded the earlier wrapper; do not interrupt its live source or pretend a disk edit updates its memory.

Queue one dormant posttest continuation, which waits for the source controller's normal exit before making any provider calls. It then serially repairs only transport-interrupted K1/Q/K2 stages across this fixed 30-cell schedule, retaining the original result and all raw files. Reuse the original thread, copy its cumulative stage calculator audit, skip successful questions and run only any already-planned recommendation retest that never executed. Never rerun laboratory sources. Preserve source shortfalls and scientific quality failures. Recompute prediction evaluation from sealed answers; if the public baseline adapter rejects a retained campaign-resource rejection, mark that baseline unavailable with its exception rather than erasing the answers or altering physics. An earlier selected recipe may be reconstructed from the exact source prefix ending at that batch. The same frozen design, thresholds and effective denominators remain; this corrects execution/interpretation without new scientific selection.
