{
  "description": "Private evaluation split placeholder. Production leaderboard parameters should be held outside the public repository.",
  "env_id": "BatchReactorWorld",
  "world_split": "private-eval",
  "default_budget": 30,
  "default_objective": "balanced",
  "public_release_policy": "Do not publish production private-eval seeds or signed result manifests.",
  "production_secret": "Set CHEMWORLD_PRIVATE_EVAL_SALT outside the public repository."
}
